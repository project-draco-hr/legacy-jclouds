{
  return new LocationBuilder().scope(LocationScope.ZONE).metadata(ImmutableMap.<String,Object>of()).description(zone.getName()).id(Long.toString(zone.getId())).parent(Iterables.getOnlyElement(provider.get())).build();
}
