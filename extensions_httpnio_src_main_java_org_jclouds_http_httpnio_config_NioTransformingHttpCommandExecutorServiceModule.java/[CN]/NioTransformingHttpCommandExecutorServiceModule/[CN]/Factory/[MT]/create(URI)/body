{
  NioHttpCommandConnectionPool pool=new NioHttpCommandConnectionPool(executor,allConnections.get(),commandQueue.get(),available.get(),clientHandler.get(),ioReactor.get(),params,endPoint);
  pool.start();
  closer.addToClose(new PoolCloser(pool));
  return pool;
}
