{
  this.info=new ResourceInfoImpl(ResourceType.FOLDER,url,name,created,inProject,modified,owner,version,shared,accessed,bytes,tags,metadata,parent);
  addAll(contents);
  this.metadataItems=metadataItems;
}
