{
  AtmosObject getBlob=connection.readFile(path).get(120,TimeUnit.SECONDS);
  assertEquals(getBlob.getData() instanceof String ? getBlob.getData() : IOUtils.toString((InputStream)getBlob.getData()),compare);
  verifyMetadata(metadataValue,getBlob);
}
