{
  if (inTasks)   taskHandler.characters(ch,start,length);
  if (inHardware)   virtualHardwareHandler.characters(ch,start,length);
  if (inOs)   operatingSystemHandler.characters(ch,start,length);
  currentText.append(ch,start,length);
}
