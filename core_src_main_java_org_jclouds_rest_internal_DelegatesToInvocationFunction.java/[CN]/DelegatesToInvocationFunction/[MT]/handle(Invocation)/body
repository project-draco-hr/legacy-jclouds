{
  if (invocation.getInvokable().isAnnotationPresent(Provides.class))   return lookupValueFromGuice(invocation.getInvokable());
 else   if (invocation.getInvokable().isAnnotationPresent(Delegate.class))   return propagateContextToDelegate(invocation);
  return methodInvoker.apply(invocation);
}
