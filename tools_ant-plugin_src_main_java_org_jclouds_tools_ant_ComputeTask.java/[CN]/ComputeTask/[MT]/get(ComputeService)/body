{
  log(String.format("get name: %s",serverElement.getName()));
  SortedSet<ServerIdentity> serversThatMatch=computeService.getServerByName(serverElement.getName());
  if (serversThatMatch.size() > 0) {
    for (    ServerIdentity server : serversThatMatch) {
      log(String.format("   server id=%s, name=%s, value=%s",server.getId(),server.getName(),new Gson().toJson(computeService.getServerMetadata(server.getId()))));
    }
  }
}
