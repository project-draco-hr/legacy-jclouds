{
  checkNotNull(id,"resource id");
  Pattern pattern=Pattern.compile("^(\\w+-\\w+)\\b.*");
  Matcher matcher=pattern.matcher((String)id);
  checkArgument(matcher.find(),"no valid resource id found: " + id.toString());
  return matcher.group(1);
}
