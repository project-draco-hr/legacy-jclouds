{
  if (this == obj)   return true;
  if (obj == null)   return false;
  if (getClass() != obj.getClass())   return false;
  ClaimSet other=(ClaimSet)obj;
  return equal(claims,other.claims) && equal(this.emissionTime,other.emissionTime) && equal(this.expirationTime,other.expirationTime);
}
