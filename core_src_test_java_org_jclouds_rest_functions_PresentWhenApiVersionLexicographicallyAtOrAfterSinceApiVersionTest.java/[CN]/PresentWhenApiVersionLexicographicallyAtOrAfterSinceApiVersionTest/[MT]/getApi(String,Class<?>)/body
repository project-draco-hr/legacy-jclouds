{
  try {
    return InvocationSuccess.create(Invocation.create(Invokable.from(EC2AsyncApi.class.getDeclaredMethod("get" + name + "ApiForRegion",String.class)),ImmutableList.<Object>of("region")),"present");
  }
 catch (  Exception e) {
    throw propagate(e);
  }
}
