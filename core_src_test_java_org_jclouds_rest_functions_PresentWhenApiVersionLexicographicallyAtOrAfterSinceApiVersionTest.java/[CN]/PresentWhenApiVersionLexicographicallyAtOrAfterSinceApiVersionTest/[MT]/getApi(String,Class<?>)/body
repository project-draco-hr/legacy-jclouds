{
  try {
    return ClassMethodArgsAndReturnVal.builder().clazz(type).method(EC2AsyncApi.class.getDeclaredMethod("get" + name + "ApiForRegion",String.class)).args(new Object[]{"region"}).returnVal("present").build();
  }
 catch (  Exception e) {
    throw propagate(e);
  }
}
