{
  try {
    return ClassInvokerArgsAndReturnVal.builder().clazz(type).invoker(Invokable.from(EC2AsyncApi.class.getDeclaredMethod("get" + name + "ApiForRegion",String.class))).args(ImmutableList.<Object>of("region")).returnVal("present").build();
  }
 catch (  Exception e) {
    throw propagate(e);
  }
}
