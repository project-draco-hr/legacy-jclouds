{
  if (this == object) {
    return true;
  }
  if (object instanceof InstantiateVAppTemplateOptions) {
    final InstantiateVAppTemplateOptions other=InstantiateVAppTemplateOptions.class.cast(object);
    return equal(networkConfig,other.networkConfig) && equal(customizeOnInstantiate,other.customizeOnInstantiate) && equal(description,other.description)&& equal(deploy,other.deploy)&& equal(powerOn,other.powerOn);
  }
 else {
    return false;
  }
}
