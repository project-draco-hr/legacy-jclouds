{
  InputStream in=null;
  try {
    if (inputStream != null) {
      in=new ByteArrayInputStream(ByteStreams.toByteArray(inputStream));
    }
  }
  finally {
    Closeables.closeQuietly(inputStream);
  }
  return in;
}
