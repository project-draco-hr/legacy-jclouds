{
  super(ioWorkerExecutor,retryHandler,errorHandler,wire);
  if (utils.getMaxConnections() > 0)   System.setProperty("http.maxConnections",String.valueOf(utils.getMaxConnections()));
  this.utils=utils;
  this.verifier=verifier;
}
