{
  super(ioWorkerExecutor,retryHandler,errorHandler,wire);
  sslMap=Maps.newHashMap();
  if (globalMaxConnections > 0)   System.setProperty("http.maxConnections",String.valueOf(globalMaxConnections));
}
