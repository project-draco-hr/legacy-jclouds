{
  SDNAuthentication authentication=injector.getInstance(SDNAuthentication.class);
  String response=authentication.authenticate(appid,user,password).get(10,TimeUnit.SECONDS);
  assertNotNull(response);
}
