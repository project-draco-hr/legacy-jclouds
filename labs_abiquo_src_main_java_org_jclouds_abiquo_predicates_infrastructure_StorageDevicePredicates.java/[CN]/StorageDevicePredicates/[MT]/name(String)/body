{
  checkNotNull(names,"names must be defined");
  return new Predicate<StorageDevice>(){
    @Override public boolean apply(    final StorageDevice storageDevice){
      return Arrays.asList(names).contains(storageDevice.getName());
    }
  }
;
}
