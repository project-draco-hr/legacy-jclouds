{
  return Lists.newArrayList(Iterables.transform(getContainerToBlobs().keySet(),new Function<String,C>(){
    public C apply(    String name){
      return createContainerMetadata(name);
    }
  }
));
}
