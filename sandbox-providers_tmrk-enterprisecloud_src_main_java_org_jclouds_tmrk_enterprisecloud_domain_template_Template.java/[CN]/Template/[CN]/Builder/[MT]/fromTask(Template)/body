{
  return fromResource(in).description(in.getDescription()).operatingSystem(in.getOperatingSystem()).processor(in.getProcessor()).memory(in.getMemory()).storage(in.getStorage()).networkAdapters(in.getNetworkAdapters()).customization(in.getCustomization());
}
