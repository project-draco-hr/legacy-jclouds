{
  ExecutorService execService=Executors.newCachedThreadPool();
  JavaUrlHttpCommandExecutorService httpService=new JavaUrlHttpCommandExecutorService(execService,new DelegatingRetryHandler(),new DelegatingErrorHandler());
  executorService=new TransformingHttpCommandExecutorServiceImpl(httpService,execService);
}
