{
  ExecutorService execService=Executors.newCachedThreadPool();
  JavaUrlHttpCommandExecutorService httpService=new JavaUrlHttpCommandExecutorService(execService,new DelegatingRetryHandler(),new DelegatingErrorHandler(),new HttpWire(),1,1);
  executorService=new TransformingHttpCommandExecutorServiceImpl(httpService);
}
