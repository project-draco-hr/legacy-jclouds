{
  ExecutorService execService=Executors.newCachedThreadPool();
  JavaUrlHttpCommandExecutorService httpService=new JavaUrlHttpCommandExecutorService(execService,new DelegatingRetryHandler(uriBuilderProvider),new DelegatingErrorHandler(),new HttpWire(),new HttpUtils(0,500,1,1),null);
  executorService=new TransformingHttpCommandExecutorServiceImpl(httpService);
}
