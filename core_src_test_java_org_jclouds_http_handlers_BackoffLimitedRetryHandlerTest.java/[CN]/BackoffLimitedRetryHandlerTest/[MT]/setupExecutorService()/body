{
  ExecutorService execService=Executors.newCachedThreadPool();
  JavaUrlHttpCommandExecutorService httpService=new JavaUrlHttpCommandExecutorService(execService,execService,new DelegatingRetryHandler(),new DelegatingErrorHandler(),new HttpWire(),1,1);
  executorService=new TransformingHttpCommandExecutorServiceImpl(httpService,execService);
}
