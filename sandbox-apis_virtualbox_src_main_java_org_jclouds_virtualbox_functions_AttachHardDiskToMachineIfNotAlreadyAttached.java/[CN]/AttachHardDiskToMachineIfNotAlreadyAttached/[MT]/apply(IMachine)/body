{
  int controllerPort=0;
  int device=1;
  try {
    machine.attachDevice(controllerIDE,controllerPort,device,HardDisk,hardDisk);
    machine.saveSettings();
  }
 catch (  VBoxException e) {
    if (!alreadyAttached(e))     throw e;
  }
  return null;
}
