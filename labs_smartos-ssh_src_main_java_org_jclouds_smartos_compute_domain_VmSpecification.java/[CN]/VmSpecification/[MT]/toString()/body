{
  return Objects.toStringHelper(this).omitNullValues().add("alias",alias).add("brand",brand).add("dataset_uuid",dataset != null ? dataset.getUuid() : null).add("dns_domain",dnsDomain).add("quota",quota).add("max_physical_memory",maxPhysicalMemory).add("max_locked_memory",maxLockedMemory).add("max_swap",maxSwap).add("tmpfs",tmpFs).add("nics",nics).toString();
}
