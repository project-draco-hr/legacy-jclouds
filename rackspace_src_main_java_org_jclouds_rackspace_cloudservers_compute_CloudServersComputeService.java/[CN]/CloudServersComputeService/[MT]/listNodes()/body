{
  logger.debug(">> listing servers");
  Set<ComputeMetadata> servers=Sets.newHashSet();
  Iterables.addAll(servers,Iterables.transform(client.listServers(ListOptions.Builder.withDetails()),serverToNodeMetadata));
  logger.debug("<< list(%d)",servers.size());
  return servers;
}
