{
  if (this == obj)   return true;
  if (obj == null || !(obj instanceof GeoRegionGroup))   return false;
  GeoRegionGroup that=GeoRegionGroup.class.cast(obj);
  return equal(this.serviceName,that.serviceName) && equal(this.name,that.name) && equal(this.countries,that.countries)&& equal(this.recordSets,that.recordSets);
}
