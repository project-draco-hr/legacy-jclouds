{
  super.setupClient();
  adapter=new SoftLayerComputeServiceAdapter(context.getApi(),ProductPackageClientLiveTest.CLOUD_SERVER_PACKAGE_NAME,new SoftLayerComputeServiceAdapter.VirtualGuestHasLoginDetailsPresent(context.getApi()),60 * 60 * 1000);
}
