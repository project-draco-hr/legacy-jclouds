{
  for (  ReferenceType response : connection.listOrgs().values()) {
    ECloudOrg org=connection.getOrg(response.getHref());
    assertNotNull(response);
    assertNotNull(response.getName());
    assertNotNull(response.getHref());
    assertEquals(connection.getDataCenterOperationsClient().listDataCentersInOrg(org.getHref()),connection.getDataCenterOperationsClient().listDataCenters(org.getDataCenters().getHref()));
  }
}
