{
  return createClient(new Function<HttpRequest,HttpResponse>(){
    @Override public HttpResponse apply(    HttpRequest command){
      assertEquals(renderRequest(command),renderRequest(fn));
      return out;
    }
  }
);
}
