{
  try {
    Payload payload=Payloads.newStringPayload(Strings2.toStringAndClose(getClass().getResourceAsStream(resource)));
    payload.getContentMetadata().setContentType(contentType);
    return payload;
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
