{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  Region region=(Region)o;
  return Objects.equal(keyname,region.keyname) && Objects.equal(description,region.description);
}
