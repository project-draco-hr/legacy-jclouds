{
  OrgNetwork.Builder<?> networkBuilder=OrgNetwork.builder().fromNetwork(network).tasks(Collections.<Task>emptySet()).description("new " + network.getDescription()).configuration(getMutatedNetworkConfiguration(network.getConfiguration()));
  return networkBuilder.build();
}
