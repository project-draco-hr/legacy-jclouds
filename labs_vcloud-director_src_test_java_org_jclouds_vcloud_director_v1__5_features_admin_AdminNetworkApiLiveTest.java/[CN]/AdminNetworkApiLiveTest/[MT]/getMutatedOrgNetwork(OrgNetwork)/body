{
  OrgNetwork.Builder<?> networkBuilder=OrgNetwork.builder().fromNetwork(network).tasks(ImmutableSet.<Task>of()).description("new " + network.getDescription()).configuration(getMutatedNetworkConfiguration(network.getConfiguration()));
  return networkBuilder.build();
}
