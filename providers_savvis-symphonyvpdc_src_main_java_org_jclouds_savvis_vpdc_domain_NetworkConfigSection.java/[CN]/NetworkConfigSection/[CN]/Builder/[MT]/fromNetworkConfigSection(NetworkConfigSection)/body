{
  return fromSection(in).network(in.getNetwork()).fenceMode(in.getFenceMode()).dhcp(in.getDhcp()).gateway(in.getGateway()).netmask(in.getNetmask()).internalToExternalNATRules(in.getInternalToExternalNATRules());
}
