{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  OrgVAppTemplateLeaseSettings that=OrgVAppTemplateLeaseSettings.class.cast(o);
  return super.equals(that) && equal(deleteOnStorageLeaseExpiration,that.deleteOnStorageLeaseExpiration) && equal(storageLeaseSeconds,that.storageLeaseSeconds);
}
