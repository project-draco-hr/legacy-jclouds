{
  Set<Map.Entry<String,Blob>> entrySet=new HashSet<Map.Entry<String,Blob>>();
  for (  Blob value : values()) {
    Map.Entry<String,Blob> entry=new Entry(pathStripper.apply(value.getMetadata().getName()),value);
    entrySet.add(entry);
  }
  return entrySet;
}
