{
  if (adminContext != null) {
    setupMetadata();
  }
  Metadata metadata=vdcApi.getMetadataApi().get(lazyGetVdc().getHref());
  assertFalse(Iterables.isEmpty(metadata.getMetadataEntries()),String.format(OBJ_FIELD_REQ_LIVE,VDC,"metadata.entries"));
  Checks.checkMetadataFor(VDC,metadata);
}
