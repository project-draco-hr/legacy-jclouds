{
  try {
    return ParseUtils.clean(gson.fromJson(new InputStreamReader(stream,"UTF-8"),ImageListResponse.class).images,ParseUtils.CLEAN_IMAGE);
  }
 catch (  UnsupportedEncodingException e) {
    throw new RuntimeException("jclouds requires UTF-8 encoding",e);
  }
}
