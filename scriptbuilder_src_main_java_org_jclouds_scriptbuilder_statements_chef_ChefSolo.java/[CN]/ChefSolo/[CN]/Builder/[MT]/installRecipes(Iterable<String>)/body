{
  this.runlist.addAll(Lists.newArrayList(transform(checkNotNull(recipes,"recipes"),new Function<String,String>(){
    @Override public String apply(    String input){
      return "recipe[" + input + "]";
    }
  }
)));
  return this;
}
