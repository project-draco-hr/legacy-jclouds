{
  return HttpRequest.builder().method(method).endpoint(uri).headers(ImmutableMultimap.<String,String>builder().put("Accept",acceptType).put("x-vcloud-authorization",token).put(HttpHeaders.COOKIE,"vcloud-token=" + token).build()).payload(payloadFromResourceWithContentType(relativeFilePath,mediaType)).build();
}
