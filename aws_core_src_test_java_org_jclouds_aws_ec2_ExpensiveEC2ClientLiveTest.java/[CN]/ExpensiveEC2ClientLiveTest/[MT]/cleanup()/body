{
  if (serverId != null)   client.terminateInstances(serverId);
  if (keyPair != null)   client.deleteKeyPair(keyPair.getKeyName());
  if (securityGroupName != null)   client.deleteSecurityGroup(securityGroupName);
}
