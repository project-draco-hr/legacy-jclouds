{
  ComputeUtils.RunScriptOnNode script=utils.runScriptOnNode(nodeMetadata,"basicscript.sh",command.getBytes());
  script.setConnection(sshClient,Logger.CONSOLE);
  try {
    script.call();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
