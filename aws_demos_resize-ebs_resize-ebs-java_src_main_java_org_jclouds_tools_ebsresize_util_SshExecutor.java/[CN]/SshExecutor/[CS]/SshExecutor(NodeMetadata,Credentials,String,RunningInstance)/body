{
  this.nodeMetadata=nodeMetadata;
  this.instanceCredentials=instanceCredentials;
  this.keyPair=keyPair;
  this.instance=instance;
  this.sshClient=new JschSshClient(new InetSocketAddress(instance.getDnsName(),22),60000,instanceCredentials.account,keyPair.getBytes());
  this.utils=new ComputeUtils(null,runScriptRunning,null);
}
