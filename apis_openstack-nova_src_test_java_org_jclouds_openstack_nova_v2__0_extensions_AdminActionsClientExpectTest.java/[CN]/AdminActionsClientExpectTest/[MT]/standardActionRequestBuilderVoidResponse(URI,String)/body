{
  return HttpRequest.builder().method("POST").headers(ImmutableMultimap.of("X-Auth-Token",authToken)).payload(payloadFromStringWithContentType("{\"" + actionName + "\":null}",MediaType.APPLICATION_JSON)).endpoint(endpoint);
}
