{
  checkNotNull(names,"names must be defined");
  return new Predicate<Enterprise>(){
    @Override public boolean apply(    final Enterprise enterprise){
      return Arrays.asList(names).contains(enterprise.getName());
    }
  }
;
}
