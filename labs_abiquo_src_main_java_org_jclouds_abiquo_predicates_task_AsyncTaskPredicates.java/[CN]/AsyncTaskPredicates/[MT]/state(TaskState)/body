{
  checkNotNull(states,"states must be defined");
  return new Predicate<AsyncTask>(){
    @Override public boolean apply(    final AsyncTask task){
      return Arrays.asList(states).contains(task.getState());
    }
  }
;
}
