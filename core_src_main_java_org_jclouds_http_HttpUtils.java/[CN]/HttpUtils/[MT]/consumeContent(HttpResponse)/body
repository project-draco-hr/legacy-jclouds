{
  if (response.getContent() != null) {
    try {
      toByteArray(response.getContent());
    }
 catch (    IOException e) {
      propagate(e);
    }
 finally {
      closeQuietly(response.getContent());
    }
  }
}
