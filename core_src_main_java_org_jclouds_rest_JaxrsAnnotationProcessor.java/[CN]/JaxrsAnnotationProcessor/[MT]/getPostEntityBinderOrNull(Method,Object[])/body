{
  for (  Object arg : args) {
    if (arg instanceof PostEntityBinder)     return (PostEntityBinder)arg;
  }
  PostBinder annotation=method.getAnnotation(PostBinder.class);
  if (annotation != null) {
    return injector.getInstance(annotation.value());
  }
  return null;
}
