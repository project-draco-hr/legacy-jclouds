{
  if (this == object) {
    return true;
  }
  if (object instanceof Template) {
    final Template other=(Template)object;
    return Objects.equal(name,other.name) && Objects.equal(platform,other.platform);
  }
 else {
    return false;
  }
}
