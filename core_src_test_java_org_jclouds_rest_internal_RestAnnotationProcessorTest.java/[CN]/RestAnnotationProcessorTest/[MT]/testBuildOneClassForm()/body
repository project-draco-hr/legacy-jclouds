{
  Method oneForm=TestClassForm.class.getMethod("oneForm",String.class);
  String form=factory(TestClassForm.class).createRequest(oneForm,new Object[]{"robot"}).getPayload().toString().toString();
  assertEquals(form,"x-amz-copy-source=/robot");
}
