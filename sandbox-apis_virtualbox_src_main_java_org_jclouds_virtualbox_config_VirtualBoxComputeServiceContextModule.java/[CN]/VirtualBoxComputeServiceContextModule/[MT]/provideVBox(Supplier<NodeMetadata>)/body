{
  return new Function<Supplier<NodeMetadata>,VirtualBoxManager>(){
    @Override public VirtualBoxManager apply(    Supplier<NodeMetadata> arg0){
      return VirtualBoxManager.createInstance(arg0.get().getId());
    }
  }
;
}
