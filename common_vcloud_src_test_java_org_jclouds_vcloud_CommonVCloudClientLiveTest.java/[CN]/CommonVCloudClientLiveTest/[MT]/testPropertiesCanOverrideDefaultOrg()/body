{
  for (  Org org : orgs) {
    RestContext<S,A> newContext=null;
    try {
      newContext=createContextWithProperties(overrideDefaults(ImmutableMap.of(VCloudConstants.PROPERTY_VCLOUD_DEFAULT_ORG,org.getName())));
      assertEquals(newContext.getApi().findOrgNamed(null),org);
    }
  finally {
      newContext.close();
    }
  }
}
