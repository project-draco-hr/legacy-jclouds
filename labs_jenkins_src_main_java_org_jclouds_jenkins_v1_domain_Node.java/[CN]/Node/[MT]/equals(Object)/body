{
  if (this == object) {
    return true;
  }
  if (object instanceof Node) {
    final Node other=Node.class.cast(object);
    return equal(getName(),other.getName()) && equal(getDescription(),other.getDescription()) && equal(getJobs(),other.getJobs());
  }
 else {
    return false;
  }
}
