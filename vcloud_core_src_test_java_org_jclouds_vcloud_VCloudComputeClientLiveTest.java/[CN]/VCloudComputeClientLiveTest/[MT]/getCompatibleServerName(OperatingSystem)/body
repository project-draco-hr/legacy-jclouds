{
  String serverName=CaseFormat.UPPER_UNDERSCORE.to(CaseFormat.LOWER_CAMEL,toTest.toString()).substring(0,toTest.toString().length() - 1 <= 15 ? toTest.toString().length() - 1 : 14);
  return serverName;
}
