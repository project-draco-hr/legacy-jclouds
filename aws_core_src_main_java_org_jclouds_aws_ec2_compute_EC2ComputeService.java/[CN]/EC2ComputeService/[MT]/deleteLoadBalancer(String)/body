{
  Map<String,String> tuple=EC2Utils.getLoadBalancerNameAndRegionFromDnsName(dnsName);
  for (  String key : tuple.keySet()) {
    ec2Client.getElasticLoadBalancerServices().deleteLoadBalancerInRegion(key,tuple.get(key));
  }
}
