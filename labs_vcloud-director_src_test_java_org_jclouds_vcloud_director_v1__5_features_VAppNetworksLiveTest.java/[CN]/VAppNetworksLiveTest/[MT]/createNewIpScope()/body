{
  IpRange newIpRange=createIpRange();
  IpRanges newIpRanges=IpRanges.builder().ipRange(newIpRange).build();
  return IpScope.builder().isInherited(false).gateway("192.168.2.1").netmask("255.255.0.0").ipRanges(newIpRanges).build();
}
