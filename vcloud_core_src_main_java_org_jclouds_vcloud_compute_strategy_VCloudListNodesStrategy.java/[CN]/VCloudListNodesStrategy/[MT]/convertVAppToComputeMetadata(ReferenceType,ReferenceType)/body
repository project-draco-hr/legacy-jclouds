{
  Location location=findLocationForResourceInVDC.apply(vdc);
  return new ComputeMetadataImpl(ComputeType.NODE,resource.getHref().toASCIIString(),resource.getName(),resource.getHref().toASCIIString(),location,null,ImmutableMap.<String,String>of());
}
