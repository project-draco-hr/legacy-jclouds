{
  template=provideTemplate(injector,template);
  String imageId=config.apply(provider + ".image-id");
  if (imageId == null)   imageId=config.apply("jclouds.image-id");
  if (imageId != null)   template.imageId(imageId);
  return template;
}
