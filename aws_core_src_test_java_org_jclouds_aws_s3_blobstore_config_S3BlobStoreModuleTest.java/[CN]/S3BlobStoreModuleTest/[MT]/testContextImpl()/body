{
  BlobStoreContext<S3AsyncClient,S3Client> context=createInjector().getInstance(Key.get(new TypeLiteral<BlobStoreContext<S3AsyncClient,S3Client>>(){
  }
));
  assertEquals(context.getClass(),BlobStoreContextImpl.class);
}
