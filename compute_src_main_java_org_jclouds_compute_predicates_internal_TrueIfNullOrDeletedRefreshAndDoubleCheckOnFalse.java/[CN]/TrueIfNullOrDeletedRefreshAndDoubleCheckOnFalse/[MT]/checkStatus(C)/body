{
  if (resource == null)   return true;
  logger.trace("%s: looking for resource status %s: currently: %s",resource.getId(),deletedStatus,resource.getStatus());
  return resource.getStatus() == deletedStatus;
}
