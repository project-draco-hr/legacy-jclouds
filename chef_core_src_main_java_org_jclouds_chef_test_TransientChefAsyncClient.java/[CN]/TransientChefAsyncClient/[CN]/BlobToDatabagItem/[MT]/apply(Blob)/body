{
  try {
    return from == null ? null : new DatabagItem(from.getMetadata().getName(),Utils.toStringAndClose(from.getPayload().getInput()));
  }
 catch (  IOException e) {
    propagate(e);
    return null;
  }
}
