{
  return fromNamedResource(in).address(in.getAddress()).networkType(in.getNetworkType()).broadcastAddress(in.getBroadcastAddress()).gatewayAddress(in.getGatewayAddress()).rnatAddress(in.getRnatAddress()).ipAddresses(in.getIpAddresses());
}
