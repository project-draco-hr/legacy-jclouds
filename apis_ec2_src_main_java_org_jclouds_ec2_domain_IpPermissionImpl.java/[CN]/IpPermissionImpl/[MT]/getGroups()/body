{
  ImmutableSet.Builder<UserIdGroupPair> groups=ImmutableSet.<UserIdGroupPair>builder();
  for (  Entry<String,String> pair : userIdGroupPairs.entries())   groups.add(new UserIdGroupPair(pair.getKey(),pair.getValue()));
  return groups.build();
}
