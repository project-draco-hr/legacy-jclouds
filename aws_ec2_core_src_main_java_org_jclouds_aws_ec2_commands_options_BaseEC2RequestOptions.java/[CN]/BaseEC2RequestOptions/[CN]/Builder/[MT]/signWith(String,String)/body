{
  T options;
  try {
    options=(T)realClass.newInstance();
  }
 catch (  Exception e) {
    throw new Error("incorrect configuration, class: " + realClass + " should extend BaseEC2RequestOptions",e);
  }
  return (T)options.signWith(awsAccessKeyId,awsSecretAccessKey);
}
