{
  try {
    parameters.put(EXPIRES,URLEncoder.encode(dateService.iso8601DateFormat(checkNotNull(time,"time")),"UTF-8"));
  }
 catch (  UnsupportedEncodingException e) {
    throw new IllegalArgumentException("bad encoding on datetime: " + time,e);
  }
  return (T)this;
}
