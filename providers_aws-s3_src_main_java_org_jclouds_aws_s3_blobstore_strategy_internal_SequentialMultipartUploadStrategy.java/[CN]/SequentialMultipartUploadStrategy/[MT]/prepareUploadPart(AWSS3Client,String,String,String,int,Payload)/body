{
  String eTag=null;
  try {
    eTag=client.uploadPart(container,key,part,uploadId,chunkedPart);
  }
 catch (  KeyNotFoundException e) {
    eTag=client.uploadPart(container,key,part,uploadId,chunkedPart);
  }
  return eTag;
}
