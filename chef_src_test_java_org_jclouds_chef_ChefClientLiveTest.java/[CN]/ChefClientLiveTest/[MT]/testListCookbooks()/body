{
  for (  String cookbook : adminConnection.getApi().listCookbooks())   for (  String version : adminConnection.getApi().getVersionsOfCookbook(cookbook)) {
    System.err.printf("%s/%s:%n",cookbook,version);
    System.err.printf("%s%n",adminConnection.getApi().getCookbook(cookbook,version));
  }
}
