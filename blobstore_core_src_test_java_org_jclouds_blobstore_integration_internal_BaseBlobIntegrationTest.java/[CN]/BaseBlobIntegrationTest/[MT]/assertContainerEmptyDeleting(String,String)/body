{
  SortedSet<? extends ResourceMetadata> listing=context.getBlobStore().list(containerName).get(30,TimeUnit.SECONDS);
  assertEquals(listing.size(),0,String.format("deleting %s, we still have %s left in container %s, using encoding %s",key,listing.size(),containerName,LOCAL_ENCODING));
}
