{
  if (this == object) {
    return true;
  }
  if (object instanceof ComputerView) {
    final ComputerView other=ComputerView.class.cast(object);
    return equal(getDisplayName(),other.getDisplayName()) && equal(getBusyExecutors(),other.getBusyExecutors()) && equal(getTotalExecutors(),other.getTotalExecutors())&& equal(getComputers(),other.getComputers());
  }
 else {
    return false;
  }
}
