{
  logger.debug(">> listing servers");
  if (options == null) {
    options=GetNodesOptions.NONE;
  }
  ImmutableMap<String,? extends ComputeMetadata> map=Maps.uniqueIndex(listNodesStrategy.execute(options),METADATA_TO_ID);
  logger.debug("<< list(%d)",map.size());
  return map;
}
