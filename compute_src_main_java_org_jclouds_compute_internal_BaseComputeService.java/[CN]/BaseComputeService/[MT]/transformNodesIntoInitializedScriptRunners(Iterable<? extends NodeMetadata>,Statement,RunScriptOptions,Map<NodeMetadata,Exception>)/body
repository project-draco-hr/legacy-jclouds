{
  return filter(transformParallel(nodes,new TransformNodesIntoInitializedScriptRunners(script,options,badNodes),executor,null,logger,"transformNodesIntoInitializedScriptRunners(" + nodes + ")"),notNull());
}
