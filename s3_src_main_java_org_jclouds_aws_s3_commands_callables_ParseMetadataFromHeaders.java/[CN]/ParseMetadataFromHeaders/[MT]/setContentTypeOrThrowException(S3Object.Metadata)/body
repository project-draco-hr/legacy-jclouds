{
  String contentType=getResponse().getFirstHeaderOrNull(HttpHeaders.CONTENT_TYPE);
  if (contentType == null)   throw new HttpException(HttpHeaders.CONTENT_TYPE + " not found in headers");
 else   metaData.setContentType(contentType);
}
