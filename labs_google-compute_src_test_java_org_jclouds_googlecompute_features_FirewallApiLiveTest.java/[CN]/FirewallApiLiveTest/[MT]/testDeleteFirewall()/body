{
  assertOperationDoneSucessfully(api().delete(FIREWALL_NAME),TIME_WAIT);
  assertOperationDoneSucessfully(context.getApi().getNetworkApiForProject(getUserProject()).delete(FIREWALL_NETWORK_NAME),TIME_WAIT);
}
