{
  String message=String.format("%1$s - %2$d - timeout  %2$d",conn,conn.hashCode(),conn.getSocketTimeout());
  logger.warn(message);
  resubmitIfRequestIsReplayable(conn,new TimeoutException(message));
}
