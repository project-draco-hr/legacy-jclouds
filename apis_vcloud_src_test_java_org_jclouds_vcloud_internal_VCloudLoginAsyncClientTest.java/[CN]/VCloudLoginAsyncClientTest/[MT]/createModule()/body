{
  return new Module(){
    @Override public void configure(    Binder binder){
    }
    @SuppressWarnings("unused") @Provides @VCloudLogin Supplier<URI> provideURI(    @Provider Supplier<URI> uri){
      return uri;
    }
  }
;
}
