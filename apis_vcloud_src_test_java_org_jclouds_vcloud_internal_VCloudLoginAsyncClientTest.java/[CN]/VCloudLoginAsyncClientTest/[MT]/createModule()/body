{
  return new Module(){
    @Override public void configure(    Binder binder){
    }
    @Provides @VCloudLogin Supplier<URI> provideURI(    @Provider Supplier<URI> uri){
      return uri;
    }
  }
;
}
