{
  checkNotNull(names,"names must be defined");
  return new Predicate<SystemProperty>(){
    @Override public boolean apply(    final SystemProperty property){
      return Arrays.asList(names).contains(property.getName());
    }
  }
;
}
