{
  for (  String imageId : imagesToDeregister)   client.deregisterImageInRegion(null,imageId);
  for (  String snapshotId : snapshotsToDelete)   context.getProviderSpecificContext().getApi().getElasticBlockStoreServices().deleteSnapshotInRegion(null,snapshotId);
}
