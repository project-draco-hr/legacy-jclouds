{
  install(new BlobStoreObjectModule<AzureBlobAsyncClient,AzureBlobClient>(new TypeLiteral<AzureBlobAsyncClient>(){
  }
,new TypeLiteral<AzureBlobClient>(){
  }
));
  install(new AzureBlobModule());
}
