{
  VirtualMachine virtualMachine=VirtualMachine.builder(env.context.getApiContext(),env.virtualAppliance,env.template).cpu(2).ram(128).build();
  virtualMachine.save();
  assertNotNull(virtualMachine.getId());
  return virtualMachine;
}
