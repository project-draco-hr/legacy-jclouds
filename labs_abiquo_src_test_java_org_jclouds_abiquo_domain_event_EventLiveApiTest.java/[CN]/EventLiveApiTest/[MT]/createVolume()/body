{
  Tier tier=env.virtualDatacenter.findStorageTier(TierPredicates.name(env.tier.getName()));
  Volume volume=Volume.builder(env.context.getApiContext(),env.virtualDatacenter,tier).name(PREFIX + "Event vol").sizeInMb(32).build();
  volume.save();
  assertNotNull(volume.getId());
  return volume;
}
