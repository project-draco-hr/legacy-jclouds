{
  String name=randomName();
  env.virtualAppliance.setName(name);
  env.virtualAppliance.update();
  EventOptions options=EventOptions.builder().dateFrom(new Date()).virtualAppName(name).description("Virtual appliance '" + name + "' has been modified.").build();
  assertEvents(options);
}
