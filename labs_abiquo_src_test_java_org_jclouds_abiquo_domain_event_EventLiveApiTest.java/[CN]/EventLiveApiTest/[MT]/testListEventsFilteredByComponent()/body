{
  String name=randomName();
  env.virtualAppliance.setName(name);
  env.virtualAppliance.update();
  EventOptions options=EventOptions.builder().dateFrom(new Date()).virtualAppName(name).component(ComponentType.VIRTUAL_APPLIANCE).build();
  assertEvents(options);
}
