{
  return Predicates.not(new Predicate<OsFamilyVersion64Bit>(){
    @Override public boolean apply(    OsFamilyVersion64Bit input){
switch (input.family) {
case CENTOS:
        return input.version.matches("5.[46]") || input.version.equals("6.[2]");
case WINDOWS:
      return (input.version.equals("2008 R2 SE") || input.version.equals("2008 R2 EE")) && input.is64Bit;
default :
    return false;
}
}
}
);
}
