{
  checkNotNull(ids,"ids must be defined");
  return new Predicate<VirtualMachineTemplate>(){
    @Override public boolean apply(    final VirtualMachineTemplate template){
      return Arrays.asList(ids).contains(template.getId());
    }
  }
;
}
