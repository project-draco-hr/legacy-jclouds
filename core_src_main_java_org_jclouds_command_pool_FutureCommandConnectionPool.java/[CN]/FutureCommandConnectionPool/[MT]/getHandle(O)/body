{
  exceptionIfNotActive();
  C conn=getConnection();
  FutureCommandConnectionHandle<C,O> handle=futureCommandConnectionHandleFactory.create(command,conn);
  associateHandleWithConnection(handle,conn);
  return handle;
}
