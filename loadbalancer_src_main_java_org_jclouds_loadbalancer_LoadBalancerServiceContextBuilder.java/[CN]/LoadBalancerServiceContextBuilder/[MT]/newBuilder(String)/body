{
  ContextBuilder<?,?,?,?> builder=ContextBuilder.newBuilder(providerOrApi);
  checkArgument(builder instanceof LoadBalancerServiceContextBuilder,"type of providerOrApi[%s] is not LoadBalancerServiceContextBuilder: %s",providerOrApi,builder);
  return LoadBalancerServiceContextBuilder.class.cast(builder);
}
