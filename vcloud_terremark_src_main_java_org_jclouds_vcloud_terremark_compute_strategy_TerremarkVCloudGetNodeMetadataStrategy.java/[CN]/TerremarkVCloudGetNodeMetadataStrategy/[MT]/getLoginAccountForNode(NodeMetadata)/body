{
  String identity=null;
  if (node.getCredentials() != null)   identity=node.getCredentials().identity;
 else   if (node.getImage() != null && node.getImage().getDefaultCredentials() != null)   identity=node.getImage().getDefaultCredentials().identity;
  return identity;
}
