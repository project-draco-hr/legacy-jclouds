{
  String account=null;
  if (node.getCredentials() != null)   account=node.getCredentials().account;
 else   if (node.getImage() != null && node.getImage().getDefaultCredentials() != null)   account=node.getImage().getDefaultCredentials().account;
  return account;
}
