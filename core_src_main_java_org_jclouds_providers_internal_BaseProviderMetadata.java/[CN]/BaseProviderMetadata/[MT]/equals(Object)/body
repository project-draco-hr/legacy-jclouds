{
  if (this == o)   return true;
  if (o == null || !(o instanceof ProviderMetadata))   return false;
  ProviderMetadata<?,?,?,?> that=ProviderMetadata.class.cast(o);
  return equal(this.getId(),that.getId()) && equal(this.getName(),that.getName()) && equal(this.getApiMetadata(),that.getApiMetadata())&& equal(this.getEndpoint(),that.getEndpoint());
}
