{
  return S3ContextBuilder.newBuilder(accessKeyId,secretAccessKey).withModules(new GaeHttpCommandExecutorServiceModule(),new ServletModule(){
    @Override protected void configureServlets(){
      serve("*.s3").with(GetAllBucketsController.class);
      requestInjection(this);
    }
  }
).buildInjector();
}
