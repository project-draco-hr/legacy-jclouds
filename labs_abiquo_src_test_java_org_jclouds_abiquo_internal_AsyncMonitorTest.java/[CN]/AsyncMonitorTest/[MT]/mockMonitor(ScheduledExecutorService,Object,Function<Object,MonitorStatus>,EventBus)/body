{
  BaseMonitoringService monitorService=new BaseMonitoringService(EasyMock.createMock(RestContext.class),scheduler,100L,eventBus);
  return monitorService.new AsyncMonitor<Object>(object,function);
}
