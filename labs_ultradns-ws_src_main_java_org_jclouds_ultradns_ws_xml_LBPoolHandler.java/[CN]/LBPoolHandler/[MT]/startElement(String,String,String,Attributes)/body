{
  Map<String,String> attributes=cleanseAttributes(attrs);
  if (equalsOrSuffix(qName,"LBPoolData")) {
    pool.zoneId(attributes.get("zoneid"));
  }
 else   if (equalsOrSuffix(qName,"PoolData")) {
    Type responseMethod=attributes.containsKey("ResponseMethod") ? Type.fromValue(attributes.get("ResponseMethod")) : null;
    pool.id(attributes.get("PoolId")).name(attributes.get("PoolName")).type(Type.fromValue(attributes.get("PoolType"))).responseMethod(responseMethod);
  }
}
