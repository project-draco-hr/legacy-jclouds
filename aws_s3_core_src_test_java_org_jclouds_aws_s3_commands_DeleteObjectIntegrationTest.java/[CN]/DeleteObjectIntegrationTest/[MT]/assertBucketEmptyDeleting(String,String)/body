{
  S3Bucket listing=client.listBucket(bucketName).get(10,TimeUnit.SECONDS);
  assertEquals(listing.getContents().size(),0,String.format("deleting %s, we still have %s left in bucket %s, using encoding %s",key,listing.getContents().size(),bucketName,LOCAL_ENCODING));
}
