{
  assertEquals(details.size(),1);
  VirtualMachineMemoryUsageDetail detail=Iterables.getOnlyElement(details);
  VirtualMachineMemoryUsageDetail expected=VirtualMachineMemoryUsageDetail.builder().href(URI.create("/cloudapi/ecloud/virtualmachines/5504")).name("helloworld").type("application/vnd.tmrk.cloud.virtualMachine").usage(ResourceCapacity.builder().value(404).unit("MB").build()).utilization(100).build();
  assertEquals(detail,expected);
}
