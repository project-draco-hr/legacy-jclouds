{
  for (  ContainerMetadata data : containerMetadataList) {
    if (toFind.equals(data.getName())) {
      return data.getUrl();
    }
  }
  return blobStore.createContainer(parent,toFind).get(requestTimeoutMilliseconds,TimeUnit.MILLISECONDS);
}
