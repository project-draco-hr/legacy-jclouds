{
  Image image=createMock(Image.class);
  replay(image);
  LoginCredentials creds=new LoginCredentials("ubuntu","foo",null,false);
  assertEquals(new ReturnCredentialsBoundToImage(creds,ImmutableMap.<String,Credentials>of(),ImmutableMap.<OsFamily,LoginCredentials>of()).execute(image),creds);
  verify(image);
}
