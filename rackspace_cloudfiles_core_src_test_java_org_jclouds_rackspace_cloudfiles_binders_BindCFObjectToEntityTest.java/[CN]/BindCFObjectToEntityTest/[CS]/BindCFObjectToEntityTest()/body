{
  Injector injector=new CloudFilesContextBuilder(new CloudFilesPropertiesBuilder("id","secret").build()).buildInjector();
  blobProvider=injector.getInstance(Blob.Factory.class);
  binderProvider=injector.getInstance(Key.get(new TypeLiteral<Provider<BindCFObjectToEntity>>(){
  }
));
  blob2Object=injector.getInstance(BlobToObject.class);
}
