{
  Map<String,BlobStoreContext> contexts=ImmutableMap.<String,BlobStoreContext>of("test1",new TransientBlobStoreContextBuilder().buildBlobStoreContext(),"test2",new TransientBlobStoreContextBuilder().buildBlobStoreContext());
  for (  BlobStoreContext blobstore : contexts.values()) {
    blobstore.getAsyncBlobStore().createContainerInLocation(null,"favo").get();
  }
  return contexts;
}
