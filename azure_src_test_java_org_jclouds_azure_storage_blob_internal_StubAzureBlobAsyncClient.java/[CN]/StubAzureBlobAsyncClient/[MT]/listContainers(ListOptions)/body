{
  return immediateFuture(new BoundedHashSet<ContainerProperties>(Iterables.transform(blobStore.getContainerToBlobs().keySet(),new Function<String,ContainerProperties>(){
    public ContainerProperties apply(    String name){
      return new ContainerPropertiesImpl(URI.create("http://stub/" + name),new Date(),"",Maps.<String,String>newHashMap());
    }
  }
),null,null,null,null,null));
}
