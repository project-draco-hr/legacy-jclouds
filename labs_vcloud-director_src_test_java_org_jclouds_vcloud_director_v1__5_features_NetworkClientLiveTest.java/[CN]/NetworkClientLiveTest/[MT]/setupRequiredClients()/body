{
  networkClient=context.getApi().getNetworkClient();
  adminContext.getApi().getNetworkClient().getMetadataClient().setMetadata(toAdminUri(networkURI),"key",MetadataValue.builder().value("value").build());
}
