{
  if (this == o)   return true;
  if (o instanceof VmSpec) {
    IsoSpec other=(IsoSpec)o;
    return Objects.equal(sourcePath,other.sourcePath) && Objects.equal(installationKeySequence,other.installationKeySequence) && Objects.equal(preConfigurationUri,other.preConfigurationUri);
  }
  return false;
}
