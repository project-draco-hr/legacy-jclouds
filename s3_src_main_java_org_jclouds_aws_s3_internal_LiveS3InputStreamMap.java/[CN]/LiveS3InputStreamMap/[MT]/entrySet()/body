{
  Set<Map.Entry<String,InputStream>> entrySet=new HashSet<Map.Entry<String,InputStream>>();
  for (  String key : keySet()) {
    InputStream input=get(key);
    if (input != null)     entrySet.add(new Entry(key,input));
  }
  return entrySet;
}
