{
  checkNotNull(names,"names must be defined");
  return new Predicate<Tier>(){
    @Override public boolean apply(    final Tier tier){
      return Arrays.asList(names).contains(tier.getName());
    }
  }
;
}
