{
  ComputeServiceContext context=null;
  try {
    context=new ComputeServiceContextFactory().createContext(new ViComputeServiceContextSpec(endpoint,identity,credential));
    Set<? extends NodeMetadata> nodeMetadataSet=context.getComputeService().runNodesWithTag("MyWinServer",1);
    for (    NodeMetadata nodeMetadata : nodeMetadataSet) {
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    if (context != null)     context.close();
  }
}
