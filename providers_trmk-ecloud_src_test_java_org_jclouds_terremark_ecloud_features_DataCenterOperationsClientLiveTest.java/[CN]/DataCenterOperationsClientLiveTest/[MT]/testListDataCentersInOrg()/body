{
  for (  ReferenceType response : getApi().listOrgs()) {
    TerremarkECloudOrg org=getApi().getOrg(response.getHref());
    assertNotNull(response);
    assertNotNull(response.getName());
    assertNotNull(response.getHref());
    assertEquals(getApi().getDataCenterOperationsClient().listDataCentersInOrg(org.getHref()),getApi().getDataCenterOperationsClient().listDataCenters(org.getDataCenters().getHref()));
  }
}
