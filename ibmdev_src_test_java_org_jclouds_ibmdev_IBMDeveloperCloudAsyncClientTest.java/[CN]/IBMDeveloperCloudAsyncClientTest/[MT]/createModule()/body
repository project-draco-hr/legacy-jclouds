{
  return new IBMDeveloperCloudRestClientModule(){
    @Override protected void configure(){
      Names.bindProperties(binder(),new IBMDeveloperCloudPropertiesBuilder("foo","bar").build());
      install(new NullLoggingModule());
      super.configure();
    }
  }
;
}
