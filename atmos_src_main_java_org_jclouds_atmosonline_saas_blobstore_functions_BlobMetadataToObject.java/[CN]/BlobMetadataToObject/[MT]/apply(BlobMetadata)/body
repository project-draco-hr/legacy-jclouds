{
  UserMetadata userMd=new UserMetadata();
  if (base.getUserMetadata() != null)   userMd.getMetadata().putAll(base.getUserMetadata());
  return factory.create(blob2ContentMd.apply(base),blob2SysMd.apply(base),userMd);
}
