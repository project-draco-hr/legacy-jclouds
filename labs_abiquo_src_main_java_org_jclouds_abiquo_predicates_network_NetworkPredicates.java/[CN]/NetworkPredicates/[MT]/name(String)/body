{
  checkNotNull(names,"names must be defined");
  return new Predicate<Network<T>>(){
    @Override public boolean apply(    final Network<T> network){
      return Arrays.asList(names).contains(network.getName());
    }
  }
;
}
