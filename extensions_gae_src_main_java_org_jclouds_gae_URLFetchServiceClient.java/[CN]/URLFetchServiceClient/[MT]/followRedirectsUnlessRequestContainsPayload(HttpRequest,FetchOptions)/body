{
  if (request.getPayload() != null || request.getMethod().equals(HTTPMethod.PUT) || request.getMethod().equals(HTTPMethod.POST))   options.doNotFollowRedirects();
 else   options.followRedirects();
}
