{
  PlatformServices platform=getPlatform(context);
  RunnableHttpRequest request=platform.getScheduler().getHttpRequestFactory().create(HttpRequest.builder().endpoint(getTargetUrl(platform.getBaseUrl(),context)).method("GET").build());
  request.run();
}
