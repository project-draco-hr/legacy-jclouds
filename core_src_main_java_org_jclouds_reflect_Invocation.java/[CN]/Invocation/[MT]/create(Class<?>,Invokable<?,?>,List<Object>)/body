{
  checkArgument(invokable.getDeclaringClass().isAssignableFrom(interfaceType),"%s isn't assignable from %s",invokable.getDeclaringClass(),interfaceType);
  return new Invocation(interfaceType,invokable,args);
}
