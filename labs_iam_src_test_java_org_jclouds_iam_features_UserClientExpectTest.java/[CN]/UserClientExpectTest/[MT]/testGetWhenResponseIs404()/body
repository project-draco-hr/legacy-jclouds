{
  HttpResponse getResponse=HttpResponse.builder().statusCode(404).build();
  IAMClient clientWhenDontExist=requestSendsResponse(get,getResponse);
  assertNull(clientWhenDontExist.getUserClient().get("name"));
}
