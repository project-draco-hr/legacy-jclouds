{
  Reference resourceEntity=Iterables.find(VdcObject.getResourceEntities(),nameEquals((String)resource));
  if (resourceEntity == null)   throw new NoSuchElementException("entity " + resource + " in Vdc "+ Vdc+ ", org "+ org+ " not found in "+ VdcObject.getResourceEntities());
  return resourceEntity.getHref();
}
