{
  if (runScriptOnNode(hostId,"VBoxManage -version").getExitCode() != 0) {
    logger().debug("installing virtualbox");
    if (isOSX(hostId))     ;
 else     runScriptOnNode(hostId,"apt-get --yes install virtualbox-ose");
  }
}
