{
  return compose(convertExceptionToValue(async.getObjectInfo(container,key),KeyNotFoundException.class,null),new Function<MutableObjectInfoWithMetadata,BlobMetadata>(){
    @Override public BlobMetadata apply(    MutableObjectInfoWithMetadata from){
      return object2BlobMd.apply(from);
    }
  }
,service);
}
