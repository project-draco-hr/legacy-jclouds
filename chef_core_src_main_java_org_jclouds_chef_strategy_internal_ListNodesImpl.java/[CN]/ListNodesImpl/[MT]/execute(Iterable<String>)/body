{
  return transformParallel(toGet,new Function<String,Future<Node>>(){
    @Override public Future<Node> apply(    String from){
      return chefAsyncClient.getNode(from);
    }
  }
,userExecutor,maxTime,logger,"getting nodes");
}
