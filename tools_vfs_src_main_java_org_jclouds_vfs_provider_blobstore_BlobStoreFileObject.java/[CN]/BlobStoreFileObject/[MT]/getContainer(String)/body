{
  metadata=Iterables.find(Iterables.transform(getBlobStore().list(),resource2Directory),new Predicate<StorageMetadata>(){
    @Override public boolean apply(    StorageMetadata input){
      return input.getName().equals(container);
    }
  }
);
  logger.info(String.format("<< container: %s/%s",container,name));
}
