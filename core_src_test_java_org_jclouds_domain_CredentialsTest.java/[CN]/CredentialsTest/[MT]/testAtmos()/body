{
  Credentials creds=Credentials.parse(URI.create("compute://domain%2Fuser:Base64%3D%3D@azureblob/container-hyphen/prefix"));
  assertEquals(creds.identity,"domain/user");
  assertEquals(creds.credential,"Base64==");
}
