{
  if (this == object) {
    return true;
  }
  if (object instanceof Bandwidth) {
    Bandwidth other=(Bandwidth)object;
    return Objects.equal(today,other.today) && Objects.equal(last30Days,other.last30Days) && Objects.equal(max,other.max);
  }
 else {
    return false;
  }
}
