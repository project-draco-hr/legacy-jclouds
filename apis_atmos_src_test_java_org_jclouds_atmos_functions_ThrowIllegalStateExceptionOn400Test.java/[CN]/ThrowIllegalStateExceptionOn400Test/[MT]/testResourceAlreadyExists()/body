{
  new ThrowIllegalStateExceptionOn400(new AtmosUtils()){
    @Override protected AtmosError parseErrorFromResponse(    HttpResponseException ignore){
      return new AtmosError(1016,"Resource already exists");
    }
  }
.apply(new HttpResponseException("Resource already exists",null,HttpResponse.builder().statusCode(400).build(),(Throwable)null));
}
