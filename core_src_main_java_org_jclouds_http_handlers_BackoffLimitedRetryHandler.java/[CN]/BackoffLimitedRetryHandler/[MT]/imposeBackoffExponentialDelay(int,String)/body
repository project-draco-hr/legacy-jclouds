{
  long delayMs=(long)(50L * Math.pow(failureCound,2));
  logger.debug("Retry %1$d/%2$d after server error, delaying for %3$d ms: %4$s",failureCound,retryCountLimit,delayMs,commandDescription);
  Thread.sleep(delayMs);
}
