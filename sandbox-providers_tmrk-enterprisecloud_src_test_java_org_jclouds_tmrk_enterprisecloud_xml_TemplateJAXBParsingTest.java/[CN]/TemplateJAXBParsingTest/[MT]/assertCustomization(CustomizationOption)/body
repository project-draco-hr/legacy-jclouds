{
  CustomizationOption expected=CustomizationOption.builder().canPowerOn(false).passwordRequired(false).sshKeyRequired(true).type(CustomizationOption.CustomizationType.LINUX).build();
  assertEquals(customization,expected);
}
