{
  super(checkNotNull(resource,"resource").getType().endsWith("org+xml") ? LocationScope.REGION : LocationScope.ZONE,resource.getLocation().toASCIIString(),resource.getName(),parent);
  this.resource=resource;
}
