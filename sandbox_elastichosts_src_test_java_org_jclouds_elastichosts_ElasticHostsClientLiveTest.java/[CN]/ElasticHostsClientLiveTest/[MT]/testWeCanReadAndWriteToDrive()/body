{
  client.writeDrive(info.getUuid(),Payloads.newStringPayload("foo"));
  assertEquals(Utils.toStringAndClose(client.readDrive(info.getUuid(),ReadDriveOptions.Builder.offset(0).size(3)).getInput()),"foo");
}
