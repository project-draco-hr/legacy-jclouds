{
  DriveInfo drive=Iterables.find(client.listDriveInfo(),new Predicate<DriveInfo>(){
    @Override public boolean apply(    DriveInfo input){
      return input.getName().equals(prefix);
    }
  }
);
  client.destroyDrive(drive.getUuid());
}
