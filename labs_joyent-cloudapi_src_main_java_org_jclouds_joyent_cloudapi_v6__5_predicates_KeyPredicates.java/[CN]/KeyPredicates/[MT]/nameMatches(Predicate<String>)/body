{
  checkNotNull(name,"name must be defined");
  return new Predicate<Key>(){
    @Override public boolean apply(    Key ext){
      return name.apply(ext.getName());
    }
    @Override public String toString(){
      return "nameMatches(" + name + ")";
    }
  }
;
}
