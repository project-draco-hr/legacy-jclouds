{
  if (from.getPayload() != null)   metadata.setContentType(from.getPayload().getContentType());
  if (metadata.getContentType() == null || "application/unknown".equals(metadata.getContentType()))   throw new HttpException(HttpHeaders.CONTENT_TYPE + " not found in headers");
}
