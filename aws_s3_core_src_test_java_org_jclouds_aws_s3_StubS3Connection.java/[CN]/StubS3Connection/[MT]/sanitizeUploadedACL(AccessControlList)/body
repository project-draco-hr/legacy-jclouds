{
  for (  Grant grant : acl.getGrants()) {
    if (grant.getGrantee() instanceof EmailAddressGrantee) {
      grant.setGrantee(new CanonicalUserGrantee(acl.getOwner().getId(),acl.getOwner().getDisplayName()));
    }
  }
  return acl;
}
