{
  long slot=-1;
  long i=0;
  while (slot == -1 && i < 4) {
    if (clone.getNetworkAdapter(i).getAttachmentType().equals(networkAttachmentType))     slot=i;
    i++;
  }
  checkState(slot != -1);
  return slot;
}
