{
  checkNotNull(names,"names must be defined");
  return new Predicate<Role>(){
    @Override public boolean apply(    final Role role){
      return Arrays.asList(names).contains(role.getName());
    }
  }
;
}
