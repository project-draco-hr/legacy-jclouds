{
  checkNotNull(types,"types must be defined");
  return new Predicate<HypervisorType>(){
    @Override public boolean apply(    final HypervisorType type){
      return Arrays.asList(types).contains(type);
    }
  }
;
}
