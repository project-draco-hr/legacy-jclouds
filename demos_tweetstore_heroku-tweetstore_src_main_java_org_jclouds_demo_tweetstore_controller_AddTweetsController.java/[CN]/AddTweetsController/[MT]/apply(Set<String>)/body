{
  List<StoredTweetStatus> statuses=Lists.newArrayList();
  for (  Iterable<StoredTweetStatus> list : Iterables.transform(in,blobStoreContextToContainerResult)) {
    Iterables.addAll(statuses,list);
  }
  return statuses;
}
