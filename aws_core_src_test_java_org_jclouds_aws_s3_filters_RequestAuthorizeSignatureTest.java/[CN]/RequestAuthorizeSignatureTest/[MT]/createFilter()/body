{
  injector=Guice.createInjector(new S3RestClientModule(),new ExecutorServiceModule(sameThreadExecutor(),sameThreadExecutor()),new ParserModule(),new AbstractModule(){
    protected void configure(){
      Jsr330.bindProperties(binder(),checkNotNull(new S3PropertiesBuilder("foo","bar")).build());
      bind(UriBuilder.class).to(UriBuilderImpl.class);
    }
  }
);
  filter=injector.getInstance(RequestAuthorizeSignature.class);
}
