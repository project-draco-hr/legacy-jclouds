{
  checkNotNull(codes,"codes must be defined");
  return new Predicate<ErrorDto>(){
    @Override public boolean apply(    final ErrorDto error){
      return Arrays.asList(codes).contains(error.getCode());
    }
  }
;
}
