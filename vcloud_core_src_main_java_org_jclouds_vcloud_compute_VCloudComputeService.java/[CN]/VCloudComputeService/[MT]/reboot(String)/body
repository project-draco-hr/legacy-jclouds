{
  VApp vApp=client.getVApp(id);
  logger.debug(">> rebooting vApp(%s)",vApp.getId());
  blockUntilVAppStatusOrThrowException(vApp,client.resetVApp(vApp.getId()),"reset",ImmutableSet.of(VAppStatus.ON));
  logger.debug("<< on vApp(%s)",vApp.getId());
}
