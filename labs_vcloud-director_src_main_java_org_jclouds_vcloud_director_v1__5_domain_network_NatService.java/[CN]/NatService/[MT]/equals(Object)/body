{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  NatService that=NatService.class.cast(o);
  return super.equals(that) && equal(natType,that.natType) && equal(natRules,that.natRules)&& equal(policy,that.policy);
}
