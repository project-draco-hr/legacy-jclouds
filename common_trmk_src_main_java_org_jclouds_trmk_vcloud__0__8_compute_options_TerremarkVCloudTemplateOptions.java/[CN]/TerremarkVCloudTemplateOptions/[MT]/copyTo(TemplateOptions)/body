{
  super.copyTo(to);
  if (to instanceof TerremarkVCloudTemplateOptions) {
    TerremarkVCloudTemplateOptions eTo=TerremarkVCloudTemplateOptions.class.cast(to);
    if (noKeyPair)     eTo.noKeyPair();
    if (keyPair != null)     eTo.keyPair=keyPair;
  }
}
