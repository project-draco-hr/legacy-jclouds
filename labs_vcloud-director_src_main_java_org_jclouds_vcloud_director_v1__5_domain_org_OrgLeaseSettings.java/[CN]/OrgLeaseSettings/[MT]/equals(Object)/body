{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  OrgLeaseSettings that=OrgLeaseSettings.class.cast(o);
  return super.equals(that) && equal(deleteOnStorageLeaseExpiration,that.deleteOnStorageLeaseExpiration) && equal(deploymentLeaseSeconds,that.deploymentLeaseSeconds)&& equal(storageLeaseSeconds,that.storageLeaseSeconds);
}
