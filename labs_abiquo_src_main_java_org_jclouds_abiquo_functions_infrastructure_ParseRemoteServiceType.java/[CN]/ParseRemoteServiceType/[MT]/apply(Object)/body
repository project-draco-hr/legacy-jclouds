{
  checkArgument(checkNotNull(input,"input") instanceof RemoteServiceType,"This parser is only valid for RemoteServiceType objects");
  return ((RemoteServiceType)input).name().replaceAll("_","").toLowerCase();
}
