{
  HttpRequest requestWithSubmitter=request.toBuilder().headers(copyOfWithEntry(request.getHeaders(),HTTP_REQUEST_ORIGINATOR_HEADER,originator)).build();
  return new HttpRequestTask(httpClient,requestWithSubmitter);
}
