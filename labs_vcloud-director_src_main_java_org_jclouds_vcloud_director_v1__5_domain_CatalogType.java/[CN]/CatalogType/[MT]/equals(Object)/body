{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  CatalogType that=CatalogType.class.cast(o);
  return super.equals(that) && equal(this.owner,that.owner) && equal(this.catalogItems,that.catalogItems)&& equal(this.isPublished,that.isPublished);
}
