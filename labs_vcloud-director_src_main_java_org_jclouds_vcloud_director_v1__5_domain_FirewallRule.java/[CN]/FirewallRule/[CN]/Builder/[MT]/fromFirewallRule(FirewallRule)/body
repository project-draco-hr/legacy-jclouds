{
  return isEnabled(in.isEnabled()).description(in.getDescription()).policy(in.getPolicy()).protocols(in.getProtocols()).port(in.getPort()).destinationIp(in.getDestinationIp()).sourcePort(in.getSourcePort()).sourceIp(in.getSourceIp()).direction(in.getDirection()).enableLogging(in.isEnableLogging());
}
