{
  checkNotNull(names,"names must be defined");
  return new Predicate<StoragePool>(){
    @Override public boolean apply(    final StoragePool storagePool){
      return Arrays.asList(names).contains(storagePool.getName());
    }
  }
;
}
