{
  return Maps.transformValues(sessionSupplier.get().getOrgs(),new Function<NamedResource,NamedResource>(){
    @Override public NamedResource apply(    NamedResource from){
      return new NamedResourceImpl(from.getName(),TerremarkVCloudExpressMediaType.KEYSLIST_XML,URI.create(from.getId().toASCIIString() + "/keysList"));
    }
  }
);
}
