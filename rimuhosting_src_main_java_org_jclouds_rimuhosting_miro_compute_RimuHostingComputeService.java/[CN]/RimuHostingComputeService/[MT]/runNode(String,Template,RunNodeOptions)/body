{
  NewServerResponse serverResponse=rhClient.createServer(name,checkNotNull(imageNameMap.get(template.getImage().getOsFamily()),"os not supported: " + template.getImage().getOsFamily()),"MIRO1B");
  return new CreateNodeResponseImpl(serverResponse.getServer().getId().toString(),serverResponse.getServer().getName(),"default",null,ImmutableMap.<String,String>of(),NodeState.RUNNING,getPublicAddresses(serverResponse.getServer()),ImmutableList.<InetAddress>of(),new Credentials("root",serverResponse.getNewInstanceRequest().getCreateOptions().getPassword()),ImmutableMap.<String,String>of());
}
