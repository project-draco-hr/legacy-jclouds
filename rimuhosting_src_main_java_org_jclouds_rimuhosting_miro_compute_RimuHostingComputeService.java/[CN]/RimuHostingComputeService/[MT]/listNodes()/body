{
  Set<ComputeMetadata> serverSet=Sets.newLinkedHashSet();
  Set<Server> rhNodes=rhClient.getServerList();
  for (  Server rhNode : rhNodes) {
    serverSet.add(new ComputeMetadataImpl(ComputeType.NODE,rhNode.getId() + "",rhNode.getName(),null,null,ImmutableMap.<String,String>of()));
  }
  return serverSet;
}
