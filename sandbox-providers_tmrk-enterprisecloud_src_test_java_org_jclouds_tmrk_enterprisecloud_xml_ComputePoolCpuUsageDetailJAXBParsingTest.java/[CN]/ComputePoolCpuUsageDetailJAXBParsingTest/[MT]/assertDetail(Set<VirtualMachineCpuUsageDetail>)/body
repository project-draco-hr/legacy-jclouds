{
  assertEquals(details.size(),1);
  VirtualMachineCpuUsageDetail detail=Iterables.getOnlyElement(details);
  VirtualMachineCpuUsageDetail expected=VirtualMachineCpuUsageDetail.builder().href(URI.create("/cloudapi/ecloud/virtualmachines/5504")).name("helloworld").type("application/vnd.tmrk.cloud.virtualMachine").usage(ResourceCapacity.builder().value(43).unit("MHz").build()).utilization(2).build();
  assertEquals(detail,expected);
}
