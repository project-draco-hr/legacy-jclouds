{
  for (  Snapshot candidate : client.getSnapshotClient().listSnapshots()) {
    if (candidate.getState() == Snapshot.State.BackedUp)     return candidate;
  }
  throw new AssertionError("No suitable Snapshot found.");
}
