{
  Invocation invocation=Invocation.create(Invokable.from(WrongValidator.class.getMethod("method",Integer.class)),ImmutableList.<Object>of(55));
  new InputParamValidator(injector).validateMethodParametersOrThrow(invocation);
}
