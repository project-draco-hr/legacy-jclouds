{
  BlobStoreContext context=ContextBuilder.newBuilder(TransientApiMetadata.builder().build()).build(BlobStoreContext.class);
  String container=KeyToStoredTweetStatusTest.class.getName() + "#container";
  context.getBlobStore().createContainerInLocation(null,container);
  return context.createBlobMap(container);
}
