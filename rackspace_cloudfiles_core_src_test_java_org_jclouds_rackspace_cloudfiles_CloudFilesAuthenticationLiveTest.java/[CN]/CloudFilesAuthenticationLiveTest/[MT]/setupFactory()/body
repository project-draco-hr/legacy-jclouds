{
  injector=Guice.createInjector(new AbstractModule(){
    @Override protected void configure(){
    }
    @SuppressWarnings("unused") @Provides @Singleton protected CloudFilesAuthentication provideCloudFilesAuthentication(    RestClientFactory factory){
      return factory.create(URI.create("https://api.mosso.com"),CloudFilesAuthentication.class);
    }
  }
,new JaxrsModule(),new ExecutorServiceModule(new WithinThreadExecutorService()),new JavaUrlHttpCommandExecutorServiceModule());
}
