{
  checkNotNull(formats,"formats must be defined");
  return new Predicate<Conversion>(){
    @Override public boolean apply(    final Conversion conversion){
      return Arrays.asList(formats).contains(conversion.getTargetFormat());
    }
  }
;
}
