{
  VirtualDisk disk=VirtualDisk.builder().index(0).name("Hard Disk 1").size(Size.builder().value(10).unit("GB").build()).build();
  Disks expectedDisks=new Disks();
  expectedDisks.setVirtualDisk(disk);
  assertEquals(expectedDisks,disks);
}
