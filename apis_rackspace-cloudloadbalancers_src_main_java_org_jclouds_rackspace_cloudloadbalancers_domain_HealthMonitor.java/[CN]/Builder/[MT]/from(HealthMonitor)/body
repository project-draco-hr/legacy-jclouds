{
  return this.type(in.getType()).delay(in.getDelay()).timeout(in.getTimeout()).attemptsBeforeDeactivation(in.getAttemptsBeforeDeactivation()).bodyRegex(in.getBodyRegex()).statusRegex(in.getStatusRegex()).path(in.getPath()).hostHeader(in.getHostHeader());
}
