{
  SshClient connection=sshFactory.create(new InetSocketAddress(details.getAddresses().getPublicAddresses().first(),22),"root",pass);
  try {
    connection.connect();
    return connection.exec(command);
  }
  finally {
    if (connection != null)     connection.disconnect();
  }
}
