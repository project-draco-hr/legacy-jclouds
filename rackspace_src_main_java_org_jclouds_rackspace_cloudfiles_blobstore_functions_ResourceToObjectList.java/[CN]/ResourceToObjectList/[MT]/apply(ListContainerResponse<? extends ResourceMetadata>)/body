{
  return new ListContainerResponseImpl<ObjectInfo>(Iterables.transform(list,new Function<ResourceMetadata,ObjectInfo>(){
    public ObjectInfo apply(    ResourceMetadata from){
      return resource2ObjectMd.apply(from);
    }
  }
),list.getPath(),list.getMarker(),list.getMaxResults(),list.size() == list.getMaxResults());
}
