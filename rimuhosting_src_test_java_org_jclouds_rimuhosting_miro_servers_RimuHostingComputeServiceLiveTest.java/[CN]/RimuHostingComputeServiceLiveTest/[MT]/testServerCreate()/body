{
  CreateServerResponse server=rhServerService.createServer("test.com",Profile.SMALLEST,Image.CENTOS_53);
  assertNotNull(rhClient.getServer(Long.valueOf(server.getId())));
  rhServerService.destroyServer(server.getId());
}
