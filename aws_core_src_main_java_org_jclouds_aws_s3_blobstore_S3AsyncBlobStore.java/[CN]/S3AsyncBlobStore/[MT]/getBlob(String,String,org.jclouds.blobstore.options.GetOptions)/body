{
  GetOptions httpOptions=blob2ObjectGetOptions.apply(optionsList);
  return compose(convertExceptionToValue(async.getObject(container,key,httpOptions),KeyNotFoundException.class,null),object2Blob,service);
}
