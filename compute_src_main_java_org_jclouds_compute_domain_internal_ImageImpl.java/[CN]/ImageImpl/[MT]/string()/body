{
  return Objects.toStringHelper("").add("id",getId()).add("providerId",getProviderId()).add("name",getName()).add("os",getOperatingSystem()).add("description",getDescription()).add("version",getVersion()).add("location",getLocation()).add("status",formatStatus(this)).add("loginUser",((defaultCredentials != null) ? defaultCredentials.identity : null)).add("tags",getTags()).add("userMetadata",getUserMetadata());
}
