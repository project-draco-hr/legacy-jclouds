{
  InputStream is=getClass().getResourceAsStream("/test_list_addresses_public.json");
  ParseInetAddressListFromGsonResponse parser=new ParseInetAddressListFromGsonResponse(i.getInstance(Gson.class));
  List<InetAddress> response=parser.apply(is);
  assertEquals(response,ImmutableList.of(InetAddress.getByAddress(new byte[]{67,23,10,(byte)132}),InetAddress.getByAddress(new byte[]{67,23,10,(byte)131})));
}
