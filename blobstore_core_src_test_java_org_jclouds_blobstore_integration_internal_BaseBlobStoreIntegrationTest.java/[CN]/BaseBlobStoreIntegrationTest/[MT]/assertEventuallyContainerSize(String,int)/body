{
  assertEventually(new Runnable(){
    public void run(){
      try {
        assertEquals(context.getBlobStore().listBlobs(containerName).get(30,TimeUnit.SECONDS).size(),count);
      }
 catch (      Exception e) {
        Utils.<RuntimeException>rethrowIfRuntimeOrSameType(e);
      }
    }
  }
);
}
