{
  DiskImage image=DiskImage.builder().osName("CentOS 6.2 64bit (English)").build();
  OperatingSystem os=new DiskImageToOperatingSystem().apply(image);
  assertNotNull(os);
  assertEquals(os.getVersion(),"6.2");
}
