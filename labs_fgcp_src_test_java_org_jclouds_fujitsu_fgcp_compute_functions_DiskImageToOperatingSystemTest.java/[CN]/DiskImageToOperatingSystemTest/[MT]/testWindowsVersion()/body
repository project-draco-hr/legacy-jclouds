{
  DiskImage image=DiskImage.builder().osName("Windows Server 2008 R2 SE 64 bit").build();
  OperatingSystem os=new DiskImageToOperatingSystem().apply(image);
  assertNotNull(os);
  assertEquals(os.getVersion(),"2008 R2 SE");
}
