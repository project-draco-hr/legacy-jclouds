{
  TwitterContextBuilder builder=newBuilder();
  assertEquals(builder.getProperties().getProperty(TwitterConstants.PROPERTY_TWITTER_ENDPOINT),"http://twitter.com");
  assertEquals(builder.getProperties().getProperty(TwitterConstants.PROPERTY_TWITTER_USER),"user");
  assertEquals(builder.getProperties().getProperty(TwitterConstants.PROPERTY_TWITTER_PASSWORD),"password");
}
