{
  checkNotNull(macs,"macs must be defined");
  return new Predicate<Nic>(){
    @Override public boolean apply(    final Nic nic){
      return Arrays.asList(macs).contains(nic.getMac());
    }
  }
;
}
