{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  DhcpService that=DhcpService.class.cast(o);
  return super.equals(that) && equal(defaultLeaseTime,that.defaultLeaseTime) && equal(maxLeaseTime,that.maxLeaseTime)&& equal(ipRange,that.ipRange);
}
