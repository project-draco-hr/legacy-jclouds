{
  for (  Entry<String,String> entry : checkNotNull(launchSpecification,"launchSpecification").buildFormParameters().entries()) {
    formParameters.put("LaunchSpecification." + entry.getKey(),entry.getValue());
  }
  return this;
}
