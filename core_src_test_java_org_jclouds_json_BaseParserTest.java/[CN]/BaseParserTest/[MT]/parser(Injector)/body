{
  try {
    return (Function<HttpResponse,T>)RestAnnotationProcessor.getTransformerForMethod(getClass().getMethod("expected"),i);
  }
 catch (  Exception e) {
    throw Throwables.propagate(e);
  }
}
