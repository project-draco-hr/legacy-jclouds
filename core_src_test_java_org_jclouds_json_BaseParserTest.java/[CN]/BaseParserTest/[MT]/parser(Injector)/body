{
  try {
    return (Function<HttpResponse,T>)AsyncRestClientProxy.getTransformerForMethod(Invokable.from(getClass().getMethod("expected")),i);
  }
 catch (  Exception e) {
    throw Throwables.propagate(e);
  }
}
