{
  InstanceVolumeManager manager=new InstanceVolumeManager(accessKeyId,secretKey);
  String privateKey=Utils.toStringAndClose(new FileInputStream(pathToKeyPair));
  manager.resizeVolume(instanceId,region,new Credentials(remoteLogin,remotePassword),privateKey,newSize);
}
