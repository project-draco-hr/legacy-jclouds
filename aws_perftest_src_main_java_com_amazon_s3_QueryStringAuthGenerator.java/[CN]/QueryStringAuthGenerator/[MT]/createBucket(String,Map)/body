{
  if (!Utils.validateBucketName(bucket,callingFormat,false))   throw new IllegalArgumentException("Invalid S3Bucket Name: " + bucket);
  Map pathArgs=new HashMap();
  return generateURL("PUT",bucket,"",pathArgs,headers);
}
