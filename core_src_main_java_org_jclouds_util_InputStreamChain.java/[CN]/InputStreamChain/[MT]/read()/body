{
  int bit=current.read();
  if (bit == -1 && streams.size() > 0) {
    try {
      current.close();
    }
 catch (    final IOException e) {
      e.printStackTrace();
    }
    current=streams.removeFirst();
    bit=read();
  }
  return bit;
}
