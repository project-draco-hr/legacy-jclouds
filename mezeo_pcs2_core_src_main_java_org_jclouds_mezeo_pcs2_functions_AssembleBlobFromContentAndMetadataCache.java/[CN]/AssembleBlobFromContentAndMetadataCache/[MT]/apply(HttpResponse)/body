{
  FileMetadata metadata=cache.get(new Key(request.getArgs()[0].toString(),request.getArgs()[1].toString()));
  PCSFile blob=new PCSFile(metadata);
  blob.setData(from.getContent());
  blob.setContentLength(metadata.getSize());
  return blob;
}
