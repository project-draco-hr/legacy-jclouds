{
  ComputeServiceContext<GleSYSClient,GleSYSAsyncClient> context=null;
  try {
    context=ComputeServiceContextBuilder.newBuilder(new GleSYSProviderMetadata()).overrides(setupProperties()).modules(setupModules()).build();
    assertEquals(context.getComputeService().listAssignableLocations().size(),4);
  }
  finally {
    Closeables.closeQuietly(context);
  }
}
