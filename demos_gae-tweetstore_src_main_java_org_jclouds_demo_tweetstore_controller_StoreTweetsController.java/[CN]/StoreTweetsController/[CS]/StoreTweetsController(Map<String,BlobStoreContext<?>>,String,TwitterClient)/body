{
  this(Sets.newHashSet(Iterables.transform(contexts.values(),new Function<BlobStoreContext<?>,BlobMap>(){
    public BlobMap apply(    BlobStoreContext<?> from){
      return from.createBlobMap(container);
    }
  }
)),client);
}
