{
  if (!super.equals(o))   return false;
  Org that=Org.class.cast(o);
  return equal(id,that.id) && equal(description,that.description) && equal(fullName,that.fullName)&& equal(links,that.links);
}
