{
  if (content instanceof String && this.getFirstHeaderOrNull(HttpHeaders.CONTENT_LENGTH) == null) {
    getHeaders().put(HttpHeaders.CONTENT_LENGTH,content.toString().getBytes().length + "");
  }
  this.entity=content;
}
