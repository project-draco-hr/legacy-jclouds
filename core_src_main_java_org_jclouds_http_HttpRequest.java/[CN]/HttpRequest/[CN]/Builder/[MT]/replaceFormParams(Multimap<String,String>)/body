{
  checkNotNull(endpoint,"endpoint");
  Multimap<String,String> map=replaceEntries(payload != null ? parseQueryToMap(payload.getRawContent().toString()) : ImmutableMultimap.<String,String>of(),parameters);
  payload=newUrlEncodedFormPayload(map);
  return self();
}
