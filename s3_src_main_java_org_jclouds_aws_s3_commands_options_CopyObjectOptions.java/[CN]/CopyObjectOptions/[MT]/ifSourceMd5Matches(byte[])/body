{
  checkState(ifNoneMatch == null,"ifMd5DoesntMatch() is not compatible with ifMd5Matches()");
  checkState(ifModifiedSince == null,"ifModifiedSince() is not compatible with ifMd5Matches()");
  this.ifMatch=String.format("\"%1s\"",S3Utils.toHexString(checkNotNull(md5,"md5")));
  return this;
}
