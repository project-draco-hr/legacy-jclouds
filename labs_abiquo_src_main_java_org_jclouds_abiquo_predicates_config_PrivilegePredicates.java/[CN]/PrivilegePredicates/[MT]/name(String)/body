{
  checkNotNull(names,"names must be defined");
  return new Predicate<Privilege>(){
    @Override public boolean apply(    final Privilege privilege){
      return Arrays.asList(names).contains(privilege.getName());
    }
  }
;
}
