{
  return id(in.getId()).name(in.getName()).type(in.getType()).state(in.getState()).dataset(in.getDataset()).memorySizeMb(in.getMemorySizeMb()).diskSizeGb(in.getDiskSizeGb()).ips(in.getIps()).metadata(in.getMetadata()).created(in.getCreated()).updated(in.getUpdated());
}
