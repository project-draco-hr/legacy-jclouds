{
  String bucketName=getBucketName();
  try {
    String objectKey="key-testDeleteObjectImpl";
    String objectValue="test";
    org.jclouds.aws.s3.domain.S3Object s3Object=new org.jclouds.aws.s3.domain.S3Object(objectKey,objectValue);
    addObjectToBucket(bucketName,s3Object);
    service.deleteObject(bucketName,objectKey);
    assertEquals(client.headObject(bucketName,objectKey),org.jclouds.aws.s3.domain.S3Object.Metadata.NOT_FOUND);
  }
  finally {
    returnBucket(bucketName);
  }
}
