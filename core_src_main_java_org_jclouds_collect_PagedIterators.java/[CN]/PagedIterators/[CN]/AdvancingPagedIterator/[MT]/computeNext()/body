{
  if (unread)   try {
    return current;
  }
  finally {
    unread=false;
  }
 else   if (nextMarker().isPresent())   return current=markerToNext.apply(nextMarker().get());
 else   return endOfData();
}
