{
  List<VmSpecification> specificationList=new ArrayList<VmSpecification>();
  VmSpecification vs=VmSpecification.builder().alias("Standard Joyent VM").nic(VmNIC.builder().simpleDHCPNic().build()).build();
  specificationList.add(vs);
  return specificationList;
}
