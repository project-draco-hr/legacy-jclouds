{
  return new Builder<Y>().method(input.getMethod()).endpoint(input.getEndpoint()).skips(input.getSkips()).filters(input.getFilters()).payload(input.getPayload()).headers(input.getHeaders()).declaring(input.getDeclaring()).javaMethod(input.getJavaMethod()).args(input.getArgs());
}
