{
  if (qName.equals("Vdc")) {
    vDC=new NamedLinkImpl(attributes.getValue(attributes.getIndex("name")),attributes.getValue(attributes.getIndex("type")),URI.create(attributes.getValue(attributes.getIndex("href"))));
  }
 else   if (qName.equals("Network")) {
    putNamedLink(availableNetworks,attributes);
  }
 else   if (qName.equals("ResourceEntity")) {
    putNamedLink(resourceEntities,attributes);
  }
}
