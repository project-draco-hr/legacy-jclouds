{
  final Set<Image> images=Sets.newHashSet();
  logger.debug(">> providing images");
  for (  org.jclouds.ibmdev.domain.Image image : sync.listImages())   images.add(new IBMImage(image,locations.get().get(image.getLocation())));
  logger.debug("<< images(%d)",images.size());
  return images;
}
