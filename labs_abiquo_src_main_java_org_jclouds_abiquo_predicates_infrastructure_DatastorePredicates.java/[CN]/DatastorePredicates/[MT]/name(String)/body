{
  checkNotNull(names,"names must be defined");
  return new Predicate<Datastore>(){
    @Override public boolean apply(    final Datastore datastore){
      return Arrays.asList(names).contains(datastore.getName());
    }
  }
;
}
