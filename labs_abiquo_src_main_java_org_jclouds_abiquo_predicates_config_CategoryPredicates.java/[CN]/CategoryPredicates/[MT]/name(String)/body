{
  checkNotNull(names,"names must be defined");
  return new Predicate<Category>(){
    @Override public boolean apply(    final Category category){
      return Arrays.asList(names).contains(category.getName());
    }
  }
;
}
