{
  return new MapMaker().expiration(seconds,TimeUnit.SECONDS).makeComputingMap(new Function<String,VCloudXSession>(){
    public VCloudXSession apply(    String key){
      return login.login();
    }
  }
);
}
