{
  return new Function<String,String>(){
    @Override public String apply(    String osDescription){
      OsFamily family=osFamily().apply(osDescription);
      if (family.equals(OsFamily.LINUX))       return parseVersion(osDescription,UBUNTU);
 else       return "";
    }
  }
;
}
