{
  return new Function<String,OsFamily>(){
    @Override public OsFamily apply(    String osDescription){
      if (osDescription.startsWith("linux"))       return OsFamily.LINUX;
      return OsFamily.UNRECOGNIZED;
    }
  }
;
}
