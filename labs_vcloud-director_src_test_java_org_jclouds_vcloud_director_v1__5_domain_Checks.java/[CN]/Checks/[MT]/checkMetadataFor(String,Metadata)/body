{
  for (  MetadataEntry entry : metadata.getMetadataEntries()) {
    assertNotNull(entry.getKey(),String.format(OBJ_FIELD_ATTRB_REQ,client,"MetadataEntry",entry.getKey(),"key"));
    assertNotNull(entry.getValue(),String.format(OBJ_FIELD_ATTRB_REQ,client,"MetadataEntry",entry.getValue(),"value"));
    checkResourceType(entry);
  }
  checkResourceType(metadata);
}
