{
  for (  MetadataEntry entry : metadata.getMetadataEntries()) {
    assertNotNull(entry.getKey(),String.format(OBJ_FIELD_ATTRB_REQ,api,"MetadataEntry",entry.getKey(),"key"));
    assertNotNull(entry.getValue(),String.format(OBJ_FIELD_ATTRB_REQ,api,"MetadataEntry",entry.getValue(),"value"));
    checkResourceType(entry);
  }
  checkResourceType(metadata);
}
