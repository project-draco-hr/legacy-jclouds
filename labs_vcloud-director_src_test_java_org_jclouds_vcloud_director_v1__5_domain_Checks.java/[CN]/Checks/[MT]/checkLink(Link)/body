{
  assertNotNull(link.getRel(),"The Rel attribute of a Link must be set");
  assertTrue(Link.Rel.ALL.contains(link.getRel()),String.format("The Rel attribute (%s) of a Link must be one of the allowed list - %s",link.getRel(),Iterables.toString(Link.Rel.ALL)));
  checkReferenceType(link);
}
