{
  if (that instanceof Size) {
    Size thatSize=Size.class.cast(that);
    return ComparisonChain.start().compare(this.getCores(),thatSize.getCores()).compare(this.getRam(),thatSize.getRam()).compare(this.getDisk(),thatSize.getDisk()).result();
  }
 else {
    return super.compareTo(that);
  }
}
