{
  checkArgument(toBind instanceof String,"this binder is only valid for Strings!");
  return super.decorateRequest(request,ImmutableMap.of("server",ImmutableMap.of("adminPass",checkNotNull(toBind,"adminPass"))));
}
