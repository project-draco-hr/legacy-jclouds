{
  for (  BlobStoreContext<?,?> context : contexts) {
    System.err.printf("adding files to container %s at %s%n",container,context.getEndPoint());
    context.createInputStreamMap(container + "/" + path).putAllStrings(fiveStrings);
  }
}
