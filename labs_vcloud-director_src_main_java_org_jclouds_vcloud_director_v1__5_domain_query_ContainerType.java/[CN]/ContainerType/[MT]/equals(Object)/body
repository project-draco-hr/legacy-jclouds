{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  ContainerType that=ContainerType.class.cast(o);
  return super.equals(that) && equal(this.name,that.name) && equal(this.page,that.page)&& equal(this.pageSize,that.pageSize)&& equal(this.total,that.total);
}
