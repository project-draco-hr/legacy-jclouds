{
  Iterable<VirtualMachineTemplate> templates=strategy.execute(env.defaultEnterprise,VirtualMachineTemplatePredicates.name(env.template.getName()));
  assertNotNull(templates);
  assertTrue(size(templates) > 0);
}
