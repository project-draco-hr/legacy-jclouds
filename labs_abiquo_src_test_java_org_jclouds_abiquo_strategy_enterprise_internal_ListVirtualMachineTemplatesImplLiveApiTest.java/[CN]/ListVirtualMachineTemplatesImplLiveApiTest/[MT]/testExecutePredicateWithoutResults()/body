{
  Iterable<VirtualMachineTemplate> templates=strategy.execute(env.defaultEnterprise,VirtualMachineTemplatePredicates.name("UNEXISTING"));
  assertNotNull(templates);
  assertEquals(size(templates),0);
}
