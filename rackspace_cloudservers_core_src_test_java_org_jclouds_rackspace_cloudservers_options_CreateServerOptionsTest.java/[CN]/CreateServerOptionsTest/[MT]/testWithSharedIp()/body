{
  CreateServerOptions options=new CreateServerOptions();
  options.withSharedIpGroup(3).withSharedIp(InetAddress.getByAddress(new byte[]{127,0,0,1}));
  HttpRequest request=buildRequest(options);
  assertSharedIp(request);
}
