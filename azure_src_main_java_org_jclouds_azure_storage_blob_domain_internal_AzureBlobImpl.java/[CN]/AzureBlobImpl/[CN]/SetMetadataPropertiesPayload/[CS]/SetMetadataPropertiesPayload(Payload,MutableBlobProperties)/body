{
  super(delegate);
  this.properties=properties;
  if (properties.getContentLength() != null)   setContentLength(properties.getContentLength());
  setContentMD5(properties.getContentMD5());
  setContentType(properties.getContentType());
}
