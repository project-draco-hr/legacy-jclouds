{
  return new TransformingSetSupplier<H,Hardware>(new Supplier<Iterable<H>>(){
    @Override public Iterable<H> get(){
      return filter(adapter.listHardwareProfiles(),notNull());
    }
  }
,transformer);
}
