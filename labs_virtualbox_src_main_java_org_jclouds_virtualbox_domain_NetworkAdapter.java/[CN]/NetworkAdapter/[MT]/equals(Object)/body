{
  if (this == o)   return true;
  if (o instanceof NetworkAdapter) {
    NetworkAdapter other=(NetworkAdapter)o;
    return Objects.equal(networkAttachmentType,other.networkAttachmentType) && Objects.equal(macAddress,other.macAddress) && Objects.equal(redirectRules,other.redirectRules);
  }
  return false;
}
