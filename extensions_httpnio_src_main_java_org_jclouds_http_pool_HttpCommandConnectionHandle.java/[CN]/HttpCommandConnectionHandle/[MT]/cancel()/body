{
  if (isCompleted()) {
    return;
  }
  if (conn != null) {
    try {
      shutdownConnection();
    }
  finally {
      conn=null;
      command=null;
      maxConnections.release();
    }
  }
  completed.release();
}
