{
  org.jclouds.blobstore.options.GetOptions getOptions=httpGetOptionsConverter.apply(options);
  return Futures.compose(Futures.makeListenable(blobStore.getBlob(container,key,getOptions)),blob2Object);
}
