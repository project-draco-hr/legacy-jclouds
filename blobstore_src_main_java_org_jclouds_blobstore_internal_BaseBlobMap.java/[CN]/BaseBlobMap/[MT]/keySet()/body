{
  Set<String> keys=Sets.newHashSet();
  for (  StorageMetadata object : getAllBlobMetadata.execute(containerName,options))   if (object.getType() == StorageType.BLOB)   keys.add(pathStripper.apply(object.getName()));
  return keys;
}
