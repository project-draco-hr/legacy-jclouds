{
  HttpResponse lastBuildResponse=HttpResponse.builder().statusCode(200).payload(payloadFromResource("/lastBuild.json")).build();
  JenkinsClient clientWhenJobExists=requestSendsResponse(lastBuild,lastBuildResponse);
  assertEquals(clientWhenJobExists.getJobClient().lastBuild("ddd").toString(),new LastBuildTest().expected().toString());
}
