{
  HttpResponse getJobResponse=HttpResponse.builder().statusCode(200).payload(payloadFromResource("/job.json")).build();
  JenkinsClient clientWhenJobExists=requestSendsResponse(getJob,getJobResponse);
  assertEquals(clientWhenJobExists.getJobClient().get("ddd").toString(),new ParseJobDetailsTest().expected().toString());
}
