{
  super(String.format("Either the source path %s or the destination path %s was not found. " + "(message: %s)",sourcePath,destinationPath,message));
  this.sourcePath=sourcePath;
  this.destinationPath=destinationPath;
}
