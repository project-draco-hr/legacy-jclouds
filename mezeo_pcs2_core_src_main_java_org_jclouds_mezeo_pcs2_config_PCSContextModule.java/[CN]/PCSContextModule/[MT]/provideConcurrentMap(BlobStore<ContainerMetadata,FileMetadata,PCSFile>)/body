{
  return new MapMaker().expiration(30,TimeUnit.SECONDS).makeComputingMap(new Function<Key,FileMetadata>(){
    public FileMetadata apply(    Key from){
      return connection.blobMetadata(from.getContainer(),from.getKey());
    }
  }
);
}
