{
  URI metadataRef=URI.create("https://vcloudbeta.bluelock.com/api/media/KEY/metadata/KEY");
  VCloudDirectorClient client=requestsSendResponses(loginRequest,sessionResponse,getStandardRequest("GET",metadataRef),getStandardPayloadResponse("/media/metadataEntry.xml",VCloudDirectorMediaType.METADATAENTRY_XML));
  MetadataEntry expected=MetadataEntry.builder().build();
  assertEquals(client.getMediaClient().getMetadataEntry(metadataRef),expected);
}
