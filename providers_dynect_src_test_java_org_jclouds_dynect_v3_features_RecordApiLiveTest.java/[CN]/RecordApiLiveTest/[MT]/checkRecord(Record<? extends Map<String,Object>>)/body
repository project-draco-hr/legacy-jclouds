{
  checkRecordId(record);
  assertTrue(record.getRData().size() > 0,"RData entries should be present for cannot be zero for Record: " + record);
  checkNotNull(record.getTTL(),"TTL cannot be null for RecordId: %s",record);
}
