{
  OrgNetwork.Builder<?> networkBuilder=OrgNetwork.builder().fromNetwork(network).tasks(null).description("new " + network.getDescription()).configuration(getMutatedNetworkConfiguration(network.getConfiguration()));
  return networkBuilder.build();
}
