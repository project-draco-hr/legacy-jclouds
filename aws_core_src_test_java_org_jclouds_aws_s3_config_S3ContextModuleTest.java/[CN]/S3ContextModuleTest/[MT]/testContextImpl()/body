{
  RestContext<S3AsyncClient,S3Client> handler=createInjector().getInstance(Key.get(new TypeLiteral<RestContext<S3AsyncClient,S3Client>>(){
  }
));
  assertEquals(handler.getClass(),RestContextImpl.class);
}
