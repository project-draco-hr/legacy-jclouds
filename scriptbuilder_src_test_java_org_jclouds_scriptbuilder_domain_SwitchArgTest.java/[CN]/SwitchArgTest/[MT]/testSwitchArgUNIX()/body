{
  assertEquals(new SwitchArg(1,ImmutableMap.of("0",interpret("echo hello zero{lf}"),"1",interpret("echo hello one{lf}"))).render(OsFamily.UNIX),"case $1 in\n0)\n   echo hello zero\n   ;;\n1)\n   echo hello one\n   ;;\nesac\n");
}
