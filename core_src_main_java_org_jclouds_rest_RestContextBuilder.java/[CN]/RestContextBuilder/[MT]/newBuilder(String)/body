{
  ContextBuilder<?,?,?,?> builder=ContextBuilder.newBuilder(providerOrApi);
  checkArgument(builder instanceof RestContextBuilder,"type of providerOrApi[%s] is not RestContextBuilder: %s",providerOrApi,builder);
  return RestContextBuilder.class.cast(builder);
}
