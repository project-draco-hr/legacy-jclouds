{
  super.setUpResourcesOnThisThread(testContext);
  exec=Executors.newCachedThreadPool();
  s3=new AmazonS3Client(new BasicAWSCredentials(System.getProperty("test.s3.identity"),System.getProperty("test.s3.credential")));
}
