{
  Map<String,Object> postData=Maps.newLinkedHashMap();
  postData.putAll(postParams);
  postData.put("state","restart");
  if (keyName != null)   postData.put("keyName",keyName);
  super.bindToRequest(request,postData);
}
