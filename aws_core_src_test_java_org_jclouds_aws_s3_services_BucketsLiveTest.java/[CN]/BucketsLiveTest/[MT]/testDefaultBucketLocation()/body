{
  String bucketName=getContainerName();
  try {
    assertEquals(LocationConstraint.US_STANDARD,context.getApi().getBucketLocation(bucketName));
  }
  finally {
    returnContainer(bucketName);
  }
}
