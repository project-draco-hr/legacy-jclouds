{
  deleteBucket(sourceBucket);
  client.putBucketIfNotExists(sourceBucket).get(10,TimeUnit.SECONDS);
  assertEquals(client.listBucket(sourceBucket).get(10,TimeUnit.SECONDS).getContents().size(),0,"bucket " + sourceBucket + "wasn't empty");
}
