{
  T expects=expected();
  Function<HttpResponse,T> parser=getParser();
  T response=parser.apply(new HttpResponse(200,"ok",Payloads.newInputStreamPayload(getClass().getResourceAsStream(resource()))));
  compare(expects,response);
}
