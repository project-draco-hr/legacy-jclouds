{
  Set<Image> images;
  logger.debug(">> providing images");
  images=Sets.<Image>newLinkedHashSet(Iterables.transform(sync.listImages(),Functions.compose(addDefaultCredentialsToImage,cloudServersImageToImage)));
  logger.debug("<< images(%d)",images.size());
  return images;
}
