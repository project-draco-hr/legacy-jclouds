{
  return new HardwareBuilder().id(input.getName()).name(input.getName()).hypervisor("kvm").processor(new Processor(input.getGuestCpus(),1.0)).providerId(input.getId()).ram(input.getMemoryMb()).uri(input.getSelfLink()).volumes(collectVolumes(input)).build();
}
