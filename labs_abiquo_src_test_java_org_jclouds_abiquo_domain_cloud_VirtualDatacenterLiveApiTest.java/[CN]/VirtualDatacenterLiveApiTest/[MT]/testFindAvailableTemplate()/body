{
  VirtualMachineTemplate templateFound=env.virtualDatacenter.findAvailableTemplate(VirtualMachineTemplatePredicates.id(template.getId()));
  assertNotNull(template);
  assertNotNull(templateFound);
  assertEquals(templateFound.getId(),template.getId());
}
