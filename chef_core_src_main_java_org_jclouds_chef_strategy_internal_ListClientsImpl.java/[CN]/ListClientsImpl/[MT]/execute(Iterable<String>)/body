{
  return transformParallel(toGet,new Function<String,Future<Client>>(){
    @Override public Future<Client> apply(    String from){
      return chefAsyncClient.getClient(from);
    }
  }
,userExecutor,maxTime,logger,"getting clients");
}
