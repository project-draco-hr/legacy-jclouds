{
  setupCredentials();
  Properties overrides=setupProperties();
  testSession=VCloudDirectorTestSession.builder().identity(identity).credential(credential).provider(provider).overrides(overrides).endpoint(endpoint).build();
}
