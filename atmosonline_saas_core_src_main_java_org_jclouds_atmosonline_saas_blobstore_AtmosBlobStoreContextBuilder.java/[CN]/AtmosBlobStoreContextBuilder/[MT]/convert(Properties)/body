{
  for (  Entry<String,String> entry : ImmutableMap.of(PROPERTY_EMCSAAS_RETRY,PROPERTY_BLOBSTORE_RETRY,PROPERTY_EMCSAAS_TIMEOUT,PROPERTY_USER_METADATA_PREFIX).entrySet()) {
    if (props.containsKey(entry.getKey()))     props.setProperty(entry.getValue(),props.getProperty(entry.getKey()));
  }
  return props;
}
