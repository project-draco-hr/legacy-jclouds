{
  return new Predicate<OsFamilyVersion64Bit>(){
    @Override public boolean apply(    OsFamilyVersion64Bit input){
      return input.family != OsFamily.RHEL && input.family != OsFamily.SUSE && input.family != OsFamily.WINDOWS;
    }
  }
;
}
