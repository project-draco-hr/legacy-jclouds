{
  if (command.getRequest().isReplayable())   commandQueue.add(command);
 else   command.setException(new IOException(String.format("%1s: command failed and request is not replayable: %2s",command,command.getRequest())));
}
