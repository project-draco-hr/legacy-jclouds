{
  Map<String,String> tags=AWSRunningInstance.class.cast(instance).getTags();
  return super.buildInstance(instance,builder).tags(filterValues(tags,equalTo("")).keySet()).userMetadata(filterValues(tags,not(equalTo(""))));
}
