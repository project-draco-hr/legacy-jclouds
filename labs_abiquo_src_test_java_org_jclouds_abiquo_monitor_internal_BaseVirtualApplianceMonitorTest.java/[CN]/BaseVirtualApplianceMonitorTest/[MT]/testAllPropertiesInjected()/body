{
  BaseVirtualApplianceMonitor monitor=(BaseVirtualApplianceMonitor)injector.getInstance(VirtualApplianceMonitor.class);
  assertNotNull(monitor.deployMonitor);
  assertNotNull(monitor.undeployMonitor);
}
