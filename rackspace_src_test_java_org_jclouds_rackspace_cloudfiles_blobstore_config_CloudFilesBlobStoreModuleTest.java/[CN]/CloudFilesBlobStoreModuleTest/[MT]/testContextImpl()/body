{
  BlobStoreContext<CloudFilesAsyncClient,CloudFilesClient> context=createInjector().getInstance(Key.get(new TypeLiteral<BlobStoreContext<CloudFilesAsyncClient,CloudFilesClient>>(){
  }
));
  assertEquals(context.getClass(),BlobStoreContextImpl.class);
}
