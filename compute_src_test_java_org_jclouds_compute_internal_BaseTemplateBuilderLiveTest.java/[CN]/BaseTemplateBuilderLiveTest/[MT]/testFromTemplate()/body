{
  Template defaultTemplate=wrapper.getComputeService().templateBuilder().build();
  assertEquals(wrapper.getComputeService().templateBuilder().fromTemplate(defaultTemplate).build().toString(),defaultTemplate.toString());
}
