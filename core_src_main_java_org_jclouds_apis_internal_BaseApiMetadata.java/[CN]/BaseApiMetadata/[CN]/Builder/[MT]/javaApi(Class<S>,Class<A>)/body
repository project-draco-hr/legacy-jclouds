{
  this.api=checkNotNull(api,"api");
  this.asyncApi=checkNotNull(asyncApi,"asyncApi");
  if (context == null)   context(contextToken(TypeToken.of(checkNotNull(api,"api")),TypeToken.of(checkNotNull(asyncApi,"asyncApi"))));
  return this;
}
