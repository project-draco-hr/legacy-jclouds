{
  return compose(Futures.makeListenable(blobStore.blobMetadata(bucketName,key)),new Function<BlobMetadata,ObjectMetadata>(){
    @Override public ObjectMetadata apply(    BlobMetadata from){
      return blob2ObjectMetadata.apply(from);
    }
  }
);
}
