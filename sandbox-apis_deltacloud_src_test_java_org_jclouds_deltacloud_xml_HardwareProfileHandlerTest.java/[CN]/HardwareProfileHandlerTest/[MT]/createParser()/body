{
  Injector injector=Guice.createInjector(new SaxParserModule());
  ParseSax<HardwareProfile> parser=(ParseSax<HardwareProfile>)injector.getInstance(ParseSax.Factory.class).create(injector.getInstance(HardwareProfileHandler.class));
  return parser;
}
