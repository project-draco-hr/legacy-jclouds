{
  String[] parts=parseHandle(handle);
  String region=parts[0];
  String id=parts[1];
  RunningInstance runningInstance=Iterables.getOnlyElement(getAllRunningInstancesInRegion(client,region,id));
  return runningInstanceToNodeMetadata.apply(runningInstance);
}
