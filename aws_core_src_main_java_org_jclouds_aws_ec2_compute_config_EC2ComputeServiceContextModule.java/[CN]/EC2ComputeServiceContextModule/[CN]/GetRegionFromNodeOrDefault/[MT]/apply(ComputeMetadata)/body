{
  Location location=locations.get(node.getLocationId());
  Region region=location.getScope() == LocationScope.REGION ? Region.fromValue(location.getId()) : Region.fromValue(location.getParent());
  return region;
}
