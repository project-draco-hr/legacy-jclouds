{
  BlobStoreContext<CloudFilesClient> context=createInjector().getInstance(Key.get(new TypeLiteral<BlobStoreContext<CloudFilesClient>>(){
  }
));
  assertEquals(context.getClass(),BlobStoreContextImpl.class);
}
