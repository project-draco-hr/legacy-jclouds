{
  return Objects.toStringHelper(this).omitNullValues().add("serverId",serverId).add("serverName",serverName).add("ipAddress",ipAddress).add("port1",port1).add("port2",port2).add("status",status).add("now",now).add("peak",peak).toString();
}
