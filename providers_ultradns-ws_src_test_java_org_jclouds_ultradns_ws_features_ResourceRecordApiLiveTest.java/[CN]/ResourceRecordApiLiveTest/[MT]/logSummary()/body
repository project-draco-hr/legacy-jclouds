{
  getAnonymousLogger().info("zoneCount: " + zones);
  for (  Entry<Integer,AtomicLong> entry : recordTypeCounts.asMap().entrySet())   getAnonymousLogger().info(format("type: %s, count: %s",entry.getKey(),entry.getValue()));
}
