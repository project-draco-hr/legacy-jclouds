{
  assertEquals(Iterables.getOnlyElement(Providers.ofType(expectedType)),toTest);
  for (  String type : Sets.difference(allTypes,ImmutableSet.of(expectedType)))   assertEquals(Iterables.size(Providers.ofType(type)),0);
}
