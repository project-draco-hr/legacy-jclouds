{
  Injector injector=createInjector();
  RestContext<AzureBlobClient> handler=injector.getInstance(Key.get(new TypeLiteral<RestContext<AzureBlobClient>>(){
  }
));
  assertEquals(handler.getClass(),RestContextImpl.class);
}
