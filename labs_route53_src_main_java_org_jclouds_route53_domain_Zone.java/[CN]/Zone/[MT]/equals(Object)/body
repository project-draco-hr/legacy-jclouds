{
  if (this == obj)   return true;
  if (obj == null || getClass() != obj.getClass())   return false;
  Zone other=Zone.class.cast(obj);
  return equal(this.id,other.id) && equal(this.name,other.name) && equal(this.callerReference,other.callerReference);
}
