{
  for (  String parameter : mandatoryParametersForSignature) {
    checkState(request.getEndpoint().getQuery().contains(parameter),"parameter " + parameter + " is required for signature");
  }
  checkState(request.getHeaders().get(HttpHeaders.HOST) != null,"request is not ready to sign; host not present");
}
