{
  checkNotNull(nicks,"nicks must be defined");
  return new Predicate<User>(){
    @Override public boolean apply(    final User user){
      return Arrays.asList(nicks).contains(user.getNick());
    }
  }
;
}
