{
  PasswordlessSudo passwordlessSudo=new PasswordlessSudo("barack");
  assertEquals(passwordlessSudo.render(OsFamily.UNIX),"touch /etc/sudoers.d/passwordless && echo \"barack ALL = NOPASSWD: ALL\" > /etc/sudoers.d/passwordless && chmod 0440 /etc/sudoers.d/passwordless");
}
