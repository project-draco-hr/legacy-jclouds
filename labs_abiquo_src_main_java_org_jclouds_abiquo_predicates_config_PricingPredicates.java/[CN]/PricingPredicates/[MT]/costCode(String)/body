{
  checkNotNull(names,"names must be defined");
  return new Predicate<CostCode>(){
    @Override public boolean apply(    final CostCode costcode){
      return Arrays.asList(names).contains(costcode.getName());
    }
  }
;
}
