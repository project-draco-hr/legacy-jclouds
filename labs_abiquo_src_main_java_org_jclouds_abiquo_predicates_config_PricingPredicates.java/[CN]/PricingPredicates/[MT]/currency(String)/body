{
  checkNotNull(names,"names must be defined");
  return new Predicate<Currency>(){
    @Override public boolean apply(    final Currency currency){
      return Arrays.asList(names).contains(currency.getName());
    }
  }
;
}
