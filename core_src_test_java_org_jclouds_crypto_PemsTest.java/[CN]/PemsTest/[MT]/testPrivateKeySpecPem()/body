{
  String encoded=Pems.pem(KeyFactory.getInstance("RSA").generatePrivate(Pems.privateKeySpec(Payloads.newStringPayload(PRIVATE_KEY))));
  assertEquals(encoded,PRIVATE_KEY.replaceAll("\n","").replaceAll("Y-----","Y-----\n").replaceAll("-----E","\n-----E"));
}
