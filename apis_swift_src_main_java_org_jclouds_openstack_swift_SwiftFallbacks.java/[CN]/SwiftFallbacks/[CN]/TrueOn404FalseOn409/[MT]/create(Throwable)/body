{
  if (contains404(checkNotNull(t,"throwable")))   return immediateFuture(true);
  if (returnValueOnCodeOrNull(t,false,equalTo(409)) != null)   return immediateFuture(false);
  throw propagate(t);
}
