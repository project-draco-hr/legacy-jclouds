{
  this.network=new Network(checkNotNull(network,"network"),null,ImmutableSet.of(Instance.NetworkInterface.AccessConfig.builder().name("external").type(Instance.NetworkInterface.AccessConfig.Type.ONE_TO_ONE_NAT).build()));
  return this;
}
