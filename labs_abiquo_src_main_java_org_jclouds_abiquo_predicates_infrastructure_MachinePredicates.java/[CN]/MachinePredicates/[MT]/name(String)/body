{
  checkNotNull(names,"name must be defined");
  return new Predicate<Machine>(){
    @Override public boolean apply(    final Machine machine){
      return Arrays.asList(names).contains(machine.getName());
    }
  }
;
}
