{
  IMedium hardDisk=new CreateMediumIfNotAlreadyExists(manager,diskFormat,true).apply(path);
  lockMachineAndApply(manager,Write,vmName,new AttachHardDiskToMachineIfNotAlreadyAttached(controllerIDE,hardDisk,manager));
}
