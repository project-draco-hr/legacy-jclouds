{
  List<Module> modules=new ArrayList<Module>();
  AzureBlobContextBuilder builder=new AzureBlobContextBuilder(new AzureBlobPropertiesBuilder("id","secret").build());
  builder.addClientModule(modules);
  assertEquals(modules.size(),1);
  assertEquals(modules.get(0).getClass(),AzureBlobRestClientModule.class);
}
