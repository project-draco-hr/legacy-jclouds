{
  if (this == object) {
    return true;
  }
  if (object instanceof Memory) {
    Memory other=(Memory)object;
    return Objects.equal(usage,other.usage) && Objects.equal(size,other.size) && Objects.equal(unit,other.unit);
  }
 else {
    return false;
  }
}
