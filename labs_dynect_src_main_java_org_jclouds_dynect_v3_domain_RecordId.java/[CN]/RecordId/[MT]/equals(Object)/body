{
  if (this == obj)   return true;
  if (obj == null || !(obj instanceof RecordId))   return false;
  RecordId that=RecordId.class.cast(obj);
  return equal(this.zone,that.zone) && equal(this.fqdn,that.fqdn) && equal(this.type,that.type)&& equal(this.id,that.id);
}
