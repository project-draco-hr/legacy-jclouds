{
  if (this == object) {
    return true;
  }
  if (object instanceof Endpoint) {
    final Endpoint other=Endpoint.class.cast(object);
    return equal(id,other.id) && equal(region,other.region) && equal(publicURL,other.publicURL)&& equal(tenantId,other.tenantId);
  }
 else {
    return false;
  }
}
