{
  if (catalog != null) {
    try {
      catalogApi.remove(catalog.getId());
    }
 catch (    Exception e) {
      logger.warn(e,"Error deleting admin catalog '%s'",catalog.getName());
    }
  }
}
