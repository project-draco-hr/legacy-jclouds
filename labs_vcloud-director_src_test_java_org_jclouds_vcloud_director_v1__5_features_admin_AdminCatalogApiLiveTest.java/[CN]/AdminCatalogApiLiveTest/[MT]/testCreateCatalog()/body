{
  AdminCatalog newCatalog=AdminCatalog.builder().name(name("Test Catalog ")).description("created by testCreateCatalog()").build();
  catalog=catalogApi.createCatalog(orgRef.getHref(),newCatalog);
  Checks.checkAdminCatalog(catalog);
}
