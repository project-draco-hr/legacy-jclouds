{
  return new DriveInfo(status,user,autoexpanding,bits,claimed,claimType,description,uuid,driveType,encryptionCipher,encryptionKey,free,imaging,installNotes,name,os,readers,readBytes,readRequests,size,tags,type,url,use,userMetadata,writeBytes,writeRequests);
}
