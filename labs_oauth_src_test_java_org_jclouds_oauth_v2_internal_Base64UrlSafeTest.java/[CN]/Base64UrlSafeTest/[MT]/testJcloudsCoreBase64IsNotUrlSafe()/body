{
  String encoded=new String(Base64.encode(STRING_THAT_GENERATES_URL_UNSAFE_BASE64_ENCODING.getBytes(Charsets.UTF_8)),Charsets.UTF_8);
  assertTrue(encoded.contains("+"),encoded);
  assertTrue(encoded.contains("/"),encoded);
}
