{
  String encoded=CryptoStreams.base64Url(STRING_THAT_GENERATES_URL_UNSAFE_BASE64_ENCODING.getBytes(Charsets.UTF_8));
  assertTrue(!encoded.contains("+"));
  assertTrue(!encoded.contains("/"));
  assertTrue(!encoded.endsWith("="));
}
