{
  if (this == object) {
    return true;
  }
  if (object instanceof Disk) {
    Disk other=(Disk)object;
    return Objects.equal(used,other.used) && Objects.equal(size,other.size) && Objects.equal(unit,other.unit);
  }
 else {
    return false;
  }
}
