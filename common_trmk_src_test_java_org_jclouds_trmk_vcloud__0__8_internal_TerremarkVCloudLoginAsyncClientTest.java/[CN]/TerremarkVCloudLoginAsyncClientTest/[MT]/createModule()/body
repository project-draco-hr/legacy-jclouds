{
  return new Module(){
    @Override public void configure(    Binder binder){
    }
    @SuppressWarnings("unused") @Provides @VCloudLogin Supplier<URI> provideURI(    @Provider URI uri){
      return Suppliers.ofInstance(uri);
    }
  }
;
}
