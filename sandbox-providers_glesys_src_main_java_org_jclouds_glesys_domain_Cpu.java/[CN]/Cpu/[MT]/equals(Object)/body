{
  if (this == object) {
    return true;
  }
  if (object instanceof Cpu) {
    Cpu other=(Cpu)object;
    return Objects.equal(system,other.system) && Objects.equal(user,other.user) && Objects.equal(nice,other.nice)&& Objects.equal(idle,other.idle)&& Objects.equal(unit,other.unit);
  }
 else {
    return false;
  }
}
