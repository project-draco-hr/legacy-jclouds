{
  return new Vdc(href,type,links,description,tasksInProgress,id,name,allocationModel,storageCapacity,computeCapacity,resourceEntities,availableNetworks,capabilities,nicQuota,networkQuota,vmQuota,isEnabled,status);
}
