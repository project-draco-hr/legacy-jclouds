{
  PCSFile file=(PCSFile)entity;
  checkNotNull(file.getContentLength(),"contentLength");
  checkArgument(file.getContentLength() <= 5 * 1024 * 1024* 1024,"maximum size for POST request is 2GB");
  blobBinder.bindToRequest(request,file2Blob.apply(file));
}
