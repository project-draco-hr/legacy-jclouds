{
  Boolean returnVal=returnValueOnCodeOrNull(checkNotNull(t,"throwable"),true,Predicates.<Integer>or(equalTo(302),equalTo(404)));
  if (returnVal != null && returnVal)   return immediateFuture(null);
  throw propagate(t);
}
