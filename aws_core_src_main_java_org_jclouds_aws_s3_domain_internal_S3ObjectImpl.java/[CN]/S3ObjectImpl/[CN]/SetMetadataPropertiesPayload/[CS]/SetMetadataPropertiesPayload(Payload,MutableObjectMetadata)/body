{
  super(delegate);
  this.metadata=checkNotNull(metadata,"metadata");
  if (metadata.getSize() != null)   setContentLength(metadata.getSize());
  setContentMD5(metadata.getContentMD5());
  setContentType(metadata.getContentType());
}
