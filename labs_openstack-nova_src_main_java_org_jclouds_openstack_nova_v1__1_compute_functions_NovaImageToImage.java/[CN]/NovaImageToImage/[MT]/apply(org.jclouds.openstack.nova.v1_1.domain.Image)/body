{
  return new ImageBuilder().id(image.getId()).providerId(image.getId()).name(image.getName()).operatingSystem(imageToOs.apply(image)).description(image.getName()).location(defaultLocation.get()).build();
}
