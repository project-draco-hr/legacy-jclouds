{
  virtualAppliance=VirtualAppliance.builder(context.getApiContext(),virtualDatacenter).name(PREFIX + "Virtual AppAloha").build();
  virtualAppliance.save();
  assertNotNull(virtualAppliance.getId());
}
