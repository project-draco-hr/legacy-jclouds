{
  for (  Entry<String,String> header : from.getHeaders().entries()) {
    if (header.getKey() != null && header.getKey().startsWith(CloudFilesHeaders.USER_METADATA_PREFIX)) {
      metadata.getUserMetadata().put(header.getKey(),header.getValue());
    }
  }
}
