{
  try {
    return client.runScriptOnNodesMatching(runningWithTag(tag),newStringPayload(buildScript(osFamily)),overrideCredentialsWith(creds));
  }
 catch (  SshException e) {
    if (getRootCause(e).getMessage().contains("Auth fail")) {
    }
    throw e;
  }
}
