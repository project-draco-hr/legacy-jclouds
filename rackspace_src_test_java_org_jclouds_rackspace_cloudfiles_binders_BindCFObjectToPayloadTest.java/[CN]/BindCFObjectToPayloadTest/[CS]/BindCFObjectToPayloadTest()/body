{
  Injector injector=new CloudFilesContextBuilder("cloudfiles",new CloudFilesPropertiesBuilder("id","secret").build()).buildInjector();
  blobProvider=injector.getInstance(Blob.Factory.class);
  binderProvider=injector.getInstance(Key.get(new TypeLiteral<Provider<BindCFObjectToPayload>>(){
  }
));
  blob2Object=injector.getInstance(BlobToObject.class);
}
