{
  InputStream is=getClass().getResourceAsStream("/discovery.xml");
  PCSCloud.Response list=new PCSCloudResponseImpl(map);
  PCSCloud.Response result=(PCSCloud.Response)factory.create(injector.getInstance(CloudXlinkHandler.class)).parse(is);
  assertEquals(result,list);
}
