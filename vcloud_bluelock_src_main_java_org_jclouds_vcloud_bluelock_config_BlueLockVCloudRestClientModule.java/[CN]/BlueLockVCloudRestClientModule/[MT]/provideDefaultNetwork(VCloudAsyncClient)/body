{
  return Iterables.getOnlyElement(Iterables.filter(client.getDefaultVDC().get(180,TimeUnit.SECONDS).getAvailableNetworks().values(),new Predicate<NamedResource>(){
    @Override public boolean apply(    NamedResource input){
      return input.getName().endsWith("Public");
    }
  }
)).getLocation();
}
