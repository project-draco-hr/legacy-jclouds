{
  Cookbook cookbook=handler.apply(new HttpResponse(ParseCookbookFromJsonTest.class.getResourceAsStream("/tomcat-cookbook.json")));
  assertEquals(cookbook,handler.apply(new HttpResponse(Utils.toInputStream(new Gson().toJson(cookbook)))));
}
