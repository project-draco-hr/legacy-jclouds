{
  Optional<URI> endpoint=getEndpointFor(caller.getMethod(),caller.getArgs());
  if (endpoint.isPresent())   logger.trace("using endpoint %s from caller %s for %s",endpoint,caller,cma(method,args));
 else   endpoint=super.findEndpoint(method,args);
  return endpoint;
}
