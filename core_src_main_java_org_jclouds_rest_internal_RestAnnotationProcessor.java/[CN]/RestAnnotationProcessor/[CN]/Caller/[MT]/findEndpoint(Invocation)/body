{
  Optional<URI> endpoint=getEndpointFor(caller);
  if (endpoint.isPresent())   logger.trace("using endpoint %s from caller %s for %s",endpoint,caller,invocation);
 else   endpoint=super.findEndpoint(invocation);
  return endpoint;
}
