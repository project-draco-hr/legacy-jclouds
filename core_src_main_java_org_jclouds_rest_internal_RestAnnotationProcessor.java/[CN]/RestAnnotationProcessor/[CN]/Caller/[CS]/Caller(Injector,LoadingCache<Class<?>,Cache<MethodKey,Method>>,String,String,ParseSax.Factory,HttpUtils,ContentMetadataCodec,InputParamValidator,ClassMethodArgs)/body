{
  super(injector,delegationMapCache,apiVersion,buildVersion,parserFactory,utils,contentMetadataCodec,inputParamValidator,caller.getClazz());
  delegationMapCache.getUnchecked(caller.getMethod().getDeclaringClass());
  this.caller=caller;
}
