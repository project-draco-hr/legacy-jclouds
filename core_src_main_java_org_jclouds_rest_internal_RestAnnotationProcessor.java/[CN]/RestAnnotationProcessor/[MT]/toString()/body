{
  String callerString=String.format("%s.%s%s",caller.getInvokable().getOwnerType().getRawType().getSimpleName(),caller.getInvokable().getName(),caller.getArgs());
  return Objects.toStringHelper("").omitNullValues().add("caller",callerString).toString();
}
