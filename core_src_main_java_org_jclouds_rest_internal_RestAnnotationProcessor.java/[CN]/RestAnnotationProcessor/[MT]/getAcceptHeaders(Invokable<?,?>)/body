{
  Optional<Consumes> accept=Optional.fromNullable(method.getAnnotation(Consumes.class)).or(Optional.fromNullable(method.getDeclaringClass().getAnnotation(Consumes.class)));
  return (accept.isPresent()) ? ImmutableSet.copyOf(accept.get().value()) : ImmutableSet.<String>of();
}
