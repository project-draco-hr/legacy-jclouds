{
  Set<String> requests=getHttpMethods(method);
  checkState(requests.size() == 1,"You must use at least one, but no more than one http method or pathparam annotation on: %s",method);
  return get(requests,0);
}
