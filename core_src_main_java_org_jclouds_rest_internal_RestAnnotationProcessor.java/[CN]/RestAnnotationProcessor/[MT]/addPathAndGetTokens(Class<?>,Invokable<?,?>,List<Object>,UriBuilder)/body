{
  if (clazz.isAnnotationPresent(Path.class))   uriBuilder.appendPath(clazz.getAnnotation(Path.class).value());
  if (method.isAnnotationPresent(Path.class))   uriBuilder.appendPath(method.getAnnotation(Path.class).value());
  return getPathParamKeyValues(method,args);
}
