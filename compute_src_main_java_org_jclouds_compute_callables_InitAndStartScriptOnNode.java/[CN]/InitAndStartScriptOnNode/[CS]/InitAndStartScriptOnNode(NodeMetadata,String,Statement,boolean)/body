{
  this.node=checkNotNull(node,"node");
  this.init=checkNotNull(script,"script") instanceof InitBuilder ? InitBuilder.class.cast(script) : createInitScript(checkNotNull(name,"name"),script);
  this.runAsRoot=runAsRoot;
}
