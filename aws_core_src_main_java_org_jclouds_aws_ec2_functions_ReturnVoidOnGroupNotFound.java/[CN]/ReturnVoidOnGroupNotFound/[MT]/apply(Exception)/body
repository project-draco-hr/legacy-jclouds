{
  if (from instanceof AWSResponseException) {
    if (((AWSResponseException)from).getError().getCode().equals("InvalidGroup.NotFound"))     return null;
  }
  return Void.class.cast(propagateOrNull(from));
}
