{
  TokenClient client=requestsSendResponses(keystoneAuthWithUsernameAndPassword,responseWithKeystoneAccess,standardRequestBuilder(endpoint + "/v2.0/tokens/XXXXXX/endpoints").build(),standardResponseBuilder(404).build()).getTokenClient().get();
  assertTrue(client.listEndpointsForToken("XXXXXX").isEmpty());
}
