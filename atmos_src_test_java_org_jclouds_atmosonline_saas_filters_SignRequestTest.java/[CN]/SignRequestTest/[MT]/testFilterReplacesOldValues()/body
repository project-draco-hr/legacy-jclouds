{
  HttpRequest request=newRequest(inputHeaders().put(AtmosStorageHeaders.SIGNATURE,"foo").put(HttpHeaders.DATE,"foo").put(AtmosStorageHeaders.DATE,"foo").put(AtmosStorageHeaders.UID,"foo").build());
  request=filter.filter(request);
  assertEquals(request.getFirstHeaderOrNull(AtmosStorageHeaders.SIGNATURE),EXPECTED_SIGNATURE);
}
