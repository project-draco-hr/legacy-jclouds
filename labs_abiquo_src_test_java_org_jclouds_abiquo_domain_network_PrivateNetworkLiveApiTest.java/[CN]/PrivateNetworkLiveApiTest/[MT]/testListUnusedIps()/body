{
  PrivateIpsDto ipsDto=env.context.getApiContext().getApi().getCloudApi().listPrivateNetworkIps(privateNetwork.unwrap(),IpOptions.builder().limit(1).build());
  int totalIps=ipsDto.getTotalSize();
  List<PrivateIp> ips=privateNetwork.listUnusedIps();
  assertEquals(ips.size(),totalIps);
}
