{
  ParseBackupScheduleFromGsonResponse parser=new ParseBackupScheduleFromGsonResponse(i.getInstance(Gson.class));
  BackupSchedule response=parser.apply(IOUtils.toInputStream("{\"backupSchedule\":{\"enabled\" : false}}"));
  assertEquals(new BackupSchedule(),response);
}
