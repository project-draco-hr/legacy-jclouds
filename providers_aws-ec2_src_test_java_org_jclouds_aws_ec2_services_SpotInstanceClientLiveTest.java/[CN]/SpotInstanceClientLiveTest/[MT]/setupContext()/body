{
  super.setupContext();
  client=context.getProviderSpecificContext().getApi();
  activeTester=new RetryablePredicate<SpotInstanceRequest>(new SpotInstanceRequestActive(client),SPOT_DELAY_SECONDS,1,1,TimeUnit.SECONDS);
}
