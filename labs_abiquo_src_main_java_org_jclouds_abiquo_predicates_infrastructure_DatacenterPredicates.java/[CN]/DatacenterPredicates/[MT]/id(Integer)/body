{
  checkNotNull(ids,"ids must be defined");
  return new Predicate<Datacenter>(){
    @Override public boolean apply(    final Datacenter datacenter){
      return Arrays.asList(ids).contains(datacenter.getId());
    }
  }
;
}
