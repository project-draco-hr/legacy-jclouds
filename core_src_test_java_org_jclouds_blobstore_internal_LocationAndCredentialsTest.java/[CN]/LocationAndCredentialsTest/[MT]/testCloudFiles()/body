{
  LocationAndCredentials creds=LocationAndCredentials.parse("blobstore://account:h3c@cloudfiles/container-hyphen/prefix");
  assertEquals(creds.acccount,"account");
  assertEquals(creds.key,"h3c");
  assertEquals(creds.uri,URI.create("blobstore://account:h3c@cloudfiles/container-hyphen/prefix"));
}
