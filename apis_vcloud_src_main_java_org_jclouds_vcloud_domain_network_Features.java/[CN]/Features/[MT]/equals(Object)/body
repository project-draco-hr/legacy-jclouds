{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  Features that=Features.class.cast(o);
  return equal(this.dhcpService,that.dhcpService) && equal(this.firewallService,that.firewallService) && equal(this.natService,that.natService);
}
