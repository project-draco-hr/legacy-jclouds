{
  return (BlobStoreContext<AzureBlobAsyncClient,AzureBlobClient>)new BlobStoreContextFactory().createContext("azureblob",account,key,ImmutableSet.of(configurationModule,new Log4JLoggingModule()),new Properties());
}
