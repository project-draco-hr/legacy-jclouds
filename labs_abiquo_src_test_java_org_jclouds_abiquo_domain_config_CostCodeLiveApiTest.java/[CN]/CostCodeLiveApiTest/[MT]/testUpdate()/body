{
  costcode.setName(PREFIX + "costcode-updated");
  costcode.update();
  CostCode apiCostCode=env.context.getPricingService().findCostCode(PricingPredicates.costCode(PREFIX + "costcode-updated"));
  assertNotNull(apiCostCode);
  assertEquals(PREFIX + "costcode-updated",apiCostCode.getName());
}
