{
  String region=ImmutableBiMap.copyOf(map).inverse().get(uri);
  if (region == null) {
    logFactory.getLogger(getClass().getName()).warn("failed to find region for current endpoint %s in %s; choosing first: %s",uri,map,region);
    region=get(map.keySet(),0);
  }
  return region;
}
