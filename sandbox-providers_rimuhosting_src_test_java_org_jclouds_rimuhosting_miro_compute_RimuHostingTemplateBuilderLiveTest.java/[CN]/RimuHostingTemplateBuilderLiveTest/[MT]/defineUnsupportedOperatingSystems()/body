{
  return new Predicate<OsFamilyVersion64Bit>(){
    @Override public boolean apply(    OsFamilyVersion64Bit input){
      return input.family != OsFamily.UBUNTU || Float.parseFloat(input.version) > 10.04 || (!(input.is64Bit) && Float.parseFloat(input.version) < 8.10);
    }
  }
;
}
