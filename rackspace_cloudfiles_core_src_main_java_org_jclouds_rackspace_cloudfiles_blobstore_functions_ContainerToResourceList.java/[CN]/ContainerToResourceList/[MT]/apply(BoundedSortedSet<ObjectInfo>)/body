{
  return new BoundedTreeSet<ResourceMetadata>(Iterables.transform(Iterables.transform(from,object2blobMd),new Function<BlobMetadata,ResourceMetadata>(){
    public ResourceMetadata apply(    BlobMetadata arg0){
      return arg0;
    }
  }
),from.getPath(),from.getMarker(),from.getMaxResults(),from.isTruncated());
}
