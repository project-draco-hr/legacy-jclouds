{
  return ImmutableSet.copyOf(transform(hosts,new Function<String,HostAndPort>(){
    @Override public HostAndPort apply(    String from){
      return HostAndPort.fromParts(from,port);
    }
  }
));
}
