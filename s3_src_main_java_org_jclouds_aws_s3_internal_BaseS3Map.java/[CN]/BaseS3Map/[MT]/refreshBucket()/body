{
  S3Bucket currentBucket=connection.getBucket(bucket).get();
  if (currentBucket == S3Bucket.NOT_FOUND)   throw new S3RuntimeException("bucket not found: " + bucket.getName());
 else   return currentBucket;
}
