{
  checkArgument(name.matches("^[a-zA-Z][-a-zA-Z0-9]+"),"Name can use uppercase and/or lowercase letters, numbers or hyphens (-). Name may only begin with a letter.");
  checkArgument(name.length() <= 15,"A maximum of 15 characters are allowed.");
  this.name=name;
  return this;
}
