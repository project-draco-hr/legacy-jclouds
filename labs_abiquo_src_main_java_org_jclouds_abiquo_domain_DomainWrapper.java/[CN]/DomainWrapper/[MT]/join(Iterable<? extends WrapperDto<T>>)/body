{
  return concat(transform(collection,new Function<WrapperDto<T>,Collection<T>>(){
    @Override public Collection<T> apply(    WrapperDto<T> input){
      return input.getCollection();
    }
  }
));
}
