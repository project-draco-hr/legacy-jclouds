{
  if (locations.get().size() == 1)   return getOnlyElement(locations.get());
  return find(locations.get(),new Predicate<Location>(){
    @Override public boolean apply(    Location input){
      return input.getScope() == LocationScope.ZONE;
    }
  }
);
}
