{
  AddMetadataAndParseResourceIdIntoBytes function=new AddMetadataAndParseResourceIdIntoBytes(fileCache,createPCSUtil());
  function.setContext(null,new Object[]{"container",new PCSFile("key")});
  function.apply(response);
}
