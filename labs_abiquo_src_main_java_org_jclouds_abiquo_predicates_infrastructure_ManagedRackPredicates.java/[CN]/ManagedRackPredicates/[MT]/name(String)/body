{
  checkNotNull(names,"names must be defined");
  return new Predicate<ManagedRack>(){
    @Override public boolean apply(    final ManagedRack rack){
      return Arrays.asList(names).contains(rack.getName());
    }
  }
;
}
