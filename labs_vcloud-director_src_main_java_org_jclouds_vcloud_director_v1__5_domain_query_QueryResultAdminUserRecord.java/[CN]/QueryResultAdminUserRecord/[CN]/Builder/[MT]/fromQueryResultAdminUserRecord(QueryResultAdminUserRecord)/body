{
  return fromQueryResultRecordType(in).name(in.getName()).org(in.getOrg()).fullName(in.getFullName()).isEnabled(in.isEnabled()).numberOfDeployedVMs(in.getNumberOfDeployedVMs()).deployedVMQuota(in.getDeployedVMQuota()).numberOfStoredVMs(in.getNumberOfStoredVMs()).storedVMQuota(in.getStoredVMQuota()).isLdapUser(in.isLdapUser()).deployedVMQuotaRank(in.getDeployedVMQuotaRank()).storedVMQuotaRank(in.getStoredVMQuotaRank());
}
