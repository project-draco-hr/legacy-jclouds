{
  return super.string().add("name",name).add("org",org).add("fullName",fullName).add("isEnabled",isEnabled).add("numberOfDeployedVMs",numberOfDeployedVMs).add("deployedVMQuota",deployedVMQuota).add("numberOfStoredVMs",numberOfStoredVMs).add("storedVMQuota",storedVMQuota).add("isLdapUser",isLdapUser).add("deployedVMQuotaRank",deployedVMQuotaRank).add("storedVMQuotaRank",storedVMQuotaRank);
}
