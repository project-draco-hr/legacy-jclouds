{
  return new BlobStoreContextFactory().createContext(provider,ImmutableSet.of(configurationModule,new Log4JLoggingModule()),setupProperties(endpoint,apiVersion,buildVersion,identity,credential));
}
