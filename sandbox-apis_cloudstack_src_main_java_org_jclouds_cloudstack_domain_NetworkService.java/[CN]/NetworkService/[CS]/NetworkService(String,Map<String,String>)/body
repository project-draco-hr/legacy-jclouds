{
  this.name=checkNotNull(name,"name");
  ImmutableSet.Builder<Capability> internal=ImmutableSet.<Capability>builder();
  for (  Entry<String,String> capabililty : checkNotNull(capabilities,"capabilities").entrySet())   internal.add(new Capability(capabililty.getKey(),capabililty.getValue()));
  this.capabilities=internal.build();
}
