{
  final int BEFORE=-1;
  final int EQUAL=0;
  final int AFTER=1;
  if (this == that)   return EQUAL;
  if (this.getId() < that.getId())   return BEFORE;
  if (this.getId() > that.getId())   return AFTER;
  return EQUAL;
}
