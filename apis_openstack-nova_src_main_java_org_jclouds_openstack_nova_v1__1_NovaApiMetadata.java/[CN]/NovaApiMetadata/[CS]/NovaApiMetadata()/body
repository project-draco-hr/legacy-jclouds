{
  this(builder().id("openstack-nova").type(ApiType.COMPUTE).name("OpenStack Nova Diablo+ API").identityName("tenantId:user").credentialName("password").documentation(URI.create("http://api.openstack.org/")));
}
