{
  if (metadata.size() > 0) {
    client.getTagServices().createTagsInRegion(region,transform(iterable,new Function<RunningInstance,String>(){
      @Override public String apply(      RunningInstance arg0){
        return arg0.getId();
      }
    }
),metadata);
  }
  return iterable;
}
