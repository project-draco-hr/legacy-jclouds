{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  OrgGeneralSettings that=OrgGeneralSettings.class.cast(o);
  return super.equals(that) && equal(canPublishCatalogs,that.canPublishCatalogs) && equal(deployedVMQuota,that.deployedVMQuota)&& equal(storedVmQuota,that.storedVmQuota)&& equal(useServerBootSequence,that.useServerBootSequence)&& equal(delayAfterPowerOnSeconds,that.delayAfterPowerOnSeconds);
}
