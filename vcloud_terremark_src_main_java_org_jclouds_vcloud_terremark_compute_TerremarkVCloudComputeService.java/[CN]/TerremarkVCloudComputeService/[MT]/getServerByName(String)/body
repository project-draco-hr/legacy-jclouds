{
  return Sets.newTreeSet(Iterables.filter(listServers(),new Predicate<Server>(){
    @Override public boolean apply(    Server input){
      return input.getName().equalsIgnoreCase(name);
    }
  }
));
}
