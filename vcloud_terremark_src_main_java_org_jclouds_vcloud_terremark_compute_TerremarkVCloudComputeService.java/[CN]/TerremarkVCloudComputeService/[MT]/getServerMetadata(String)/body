{
  VApp vApp=tmClient.getVApp(id);
  Set<InetAddress> publicAddresses=ImmutableSet.<InetAddress>of();
  return new ServerMetadataImpl(vApp.getId(),vApp.getName(),publicAddresses,vApp.getNetworkToAddresses().values(),22,LoginType.SSH);
}
