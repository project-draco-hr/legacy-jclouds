{
  return (Engine)Iterables.find(asList((Engine[])valueOf("engines",valueOf("container",server,TomcatServerBase.class))),new Predicate<Engine>(){
    @Override public boolean apply(    Engine input){
      return input.getName().equals("localEngine");
    }
  }
);
}
