{
  Matcher matcher=TZ_PATTERN.matcher(toParse);
  if (matcher.find()) {
    String tz=matcher.group(2).replace(":","");
    if (tz.length() == 2)     tz+="00";
    return tz;
  }
 else {
    return "";
  }
}
