{
  ExecResponse returnVal=super.doCall();
  if (returnVal.getExitCode() != 0)   logger.warn("task: %s had non-zero exit status: %s",init.getInstanceName(),returnVal);
  return statusFactory.create(this).init();
}
