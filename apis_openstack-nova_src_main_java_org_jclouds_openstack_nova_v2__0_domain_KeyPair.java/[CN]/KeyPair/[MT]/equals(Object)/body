{
  if (this == obj)   return true;
  if (obj == null || getClass() != obj.getClass())   return false;
  KeyPair that=KeyPair.class.cast(obj);
  return Objects.equal(this.publicKey,that.publicKey) && Objects.equal(this.privateKey,that.privateKey) && Objects.equal(this.userId,that.userId)&& Objects.equal(this.name,that.name)&& Objects.equal(this.fingerprint,that.fingerprint);
}
