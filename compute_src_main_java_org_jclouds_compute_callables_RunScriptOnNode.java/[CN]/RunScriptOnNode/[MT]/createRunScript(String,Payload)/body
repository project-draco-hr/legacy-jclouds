{
  String path="/tmp/" + scriptName;
  InitBuilder initBuilder=new InitBuilder(scriptName,path,path,Collections.<String,String>emptyMap(),splitOnNewlines(script));
  return Payloads.newByteArrayPayload(initBuilder.build(OsFamily.UNIX).getBytes());
}
