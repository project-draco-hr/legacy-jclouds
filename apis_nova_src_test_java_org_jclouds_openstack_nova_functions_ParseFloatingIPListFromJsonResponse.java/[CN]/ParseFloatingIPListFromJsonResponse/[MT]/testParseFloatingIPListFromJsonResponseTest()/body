{
  InputStream is=getClass().getResourceAsStream("/test_list_floatingips.json");
  UnwrapOnlyJsonValue<List<FloatingIP>> parser=i.getInstance(Key.get(new TypeLiteral<UnwrapOnlyJsonValue<List<FloatingIP>>>(){
  }
));
  List<FloatingIP> response=parser.apply(HttpResponse.builder().statusCode(200).message("ok").payload(is).build());
  assertEquals(response.size(),1);
  FloatingIP floatingIP=new FloatingIP(1,"10.0.0.3","11.0.0.1",12);
  assertEquals(response.get(0),floatingIP);
}
