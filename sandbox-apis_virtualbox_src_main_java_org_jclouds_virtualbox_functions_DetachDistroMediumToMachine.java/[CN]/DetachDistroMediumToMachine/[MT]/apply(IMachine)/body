{
  try {
    int controllerPort=0;
    int device=0;
    machine.detachDevice(controller,controllerPort,device);
    machine.saveSettings();
  }
 catch (  VBoxException e) {
    if (!alreadyDetached(e))     throw e;
  }
  return null;
}
