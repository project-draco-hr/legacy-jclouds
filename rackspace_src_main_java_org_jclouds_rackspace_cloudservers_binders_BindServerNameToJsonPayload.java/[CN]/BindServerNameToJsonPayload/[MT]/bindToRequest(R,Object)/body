{
  checkArgument(toBind instanceof String,"this binder is only valid for Strings!");
  return super.bindToRequest(request,ImmutableMap.of("server",ImmutableMap.of("name",checkNotNull(toBind,"name"))));
}
