{
  Attachment volumeAttachment=elasticBlockStoreServices.attachVolumeInRegion(instance.getRegion(),volume.getId(),instance.getId(),instance.getRootDeviceName());
  checkState(volumeAttached.apply(volumeAttachment),"Couldn't attach volume back to the instance");
}
