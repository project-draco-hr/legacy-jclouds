{
  if (this == obj) {
    return true;
  }
  if ((obj == null) || (getClass() != obj.getClass())) {
    return false;
  }
  AllowedArguments that=AllowedArguments.class.cast(obj);
  return Objects.equal(this.costPerUnit,that.costPerUnit) && Objects.equal(this.allowedUnits,that.allowedUnits);
}
