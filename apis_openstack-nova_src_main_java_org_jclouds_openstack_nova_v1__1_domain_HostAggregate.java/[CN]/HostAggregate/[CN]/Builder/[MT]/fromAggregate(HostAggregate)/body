{
  return this.id(in.getId()).name(in.getName()).availabilityZone(in.getAvailabilityZone()).hosts(in.getHosts()).state(in.getState()).created(in.getCreated()).updated(in.getUpdated().orNull()).metadata(in.getMetadata());
}
