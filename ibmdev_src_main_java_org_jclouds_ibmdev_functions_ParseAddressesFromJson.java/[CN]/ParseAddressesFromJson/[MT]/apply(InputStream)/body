{
  try {
    return ParseUtils.clean(gson.fromJson(new InputStreamReader(stream,"UTF-8"),AddressListResponse.class).addresses,ParseUtils.CLEAN_ADDRESS);
  }
 catch (  UnsupportedEncodingException e) {
    throw new RuntimeException("jclouds requires UTF-8 encoding",e);
  }
}
