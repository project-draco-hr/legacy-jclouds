{
  if (qName.equals("VAppTemplate")) {
    catalog=Utils.newNamedResource(attributes);
    if (attributes.getIndex("status") != -1)     status=Status.fromValue(attributes.getValue(attributes.getIndex("status")));
  }
}
