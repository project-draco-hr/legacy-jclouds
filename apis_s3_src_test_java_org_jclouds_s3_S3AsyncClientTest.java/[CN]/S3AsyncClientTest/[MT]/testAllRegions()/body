{
  Method method=S3AsyncClient.class.getMethod("putBucketInRegion",String.class,String.class,Array.newInstance(PutBucketOptions.class,0).getClass());
  for (  String region : Region.DEFAULT_S3) {
    processor.createRequest(method,region,"bucket-" + region);
  }
}
