{
  return PaginatedIterables.lazyContinue(instanceApi.list(options),new Function<Object,PaginatedIterable<Instance>>(){
    @Override public PaginatedIterable<Instance> apply(    Object input){
      return instanceApi.list(options.clone().afterMarker(input));
    }
    @Override public String toString(){
      return "listInstances(" + options + ")";
    }
  }
);
}
