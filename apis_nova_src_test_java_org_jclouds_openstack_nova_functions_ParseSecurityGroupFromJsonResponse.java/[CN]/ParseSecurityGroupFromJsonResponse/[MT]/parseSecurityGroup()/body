{
  Injector i=Guice.createInjector(new GsonModule());
  InputStream is=ParseFloatingIPFromJsonResponse.class.getResourceAsStream("/test_get_security_group.json");
  UnwrapOnlyJsonValue<SecurityGroup> parser=i.getInstance(Key.get(new TypeLiteral<UnwrapOnlyJsonValue<SecurityGroup>>(){
  }
));
  return parser.apply(new HttpResponse(200,"ok",Payloads.newInputStreamPayload(is)));
}
