{
  return fromResource(in).status(in.getStatus()).processorCount(in.getProcessorCount()).memory(in.getMemory()).storage(in.getStorage()).operatingSystem(in.getOperatingSystem()).poweredOn(in.isPoweredOn()).toolsStatus(in.getToolsStatus());
}
