{
  return new Predicate<OsFamilyVersion64Bit>(){
    @Override public boolean apply(    OsFamilyVersion64Bit input){
switch (input.family) {
case CENTOS:
        return !(input.version.equals("") && input.is64Bit) && !(input.version.equals("5.3") && input.is64Bit);
case WINDOWS:
      return !(input.version.equals("") && input.is64Bit) && !(input.version.equals("2008") && input.is64Bit);
default :
    return true;
}
}
}
;
}
