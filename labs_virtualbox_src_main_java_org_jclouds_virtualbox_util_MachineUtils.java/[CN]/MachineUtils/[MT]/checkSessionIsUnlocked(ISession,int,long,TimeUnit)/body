{
  checkState(new RetryableNumTimesPredicate<SessionState>(new SessionStatePredicate(session),attempts,period,timeUnit).apply(SessionState.Unlocked),"timed out or number of retries(%s) reached waiting for session to be unlocked",attempts);
}
