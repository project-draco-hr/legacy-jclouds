{
  checkNotNull(addresses,"addresses must be defined");
  return new Predicate<T>(){
    @Override public boolean apply(    final T address){
      return Arrays.asList(addresses).contains(address.getIp());
    }
  }
;
}
