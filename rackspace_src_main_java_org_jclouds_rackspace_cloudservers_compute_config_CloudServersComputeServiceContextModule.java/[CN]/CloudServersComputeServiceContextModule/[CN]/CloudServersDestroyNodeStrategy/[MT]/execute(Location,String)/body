{
  int serverId=Integer.parseInt(id);
  if (!client.deleteServer(serverId))   return false;
  Server server=client.getServer(serverId);
  return server == null ? false : serverDeleted.apply(server);
}
