{
  CreateNodeResponse server=rhNodeService.createNode("test.com",Profile.SMALLEST,Image.CENTOS_53);
  assertNotNull(rhClient.getServer(Long.valueOf(server.getId())));
  rhNodeService.destroyNode(server.getId());
}
