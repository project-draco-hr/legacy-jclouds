{
  return ImmutableMap.<Slice.Status,NodeState>builder().put(Slice.Status.ACTIVE,NodeState.RUNNING).put(Slice.Status.BUILD,NodeState.PENDING).put(Slice.Status.REBOOT,NodeState.PENDING).put(Slice.Status.HARD_REBOOT,NodeState.PENDING).put(Slice.Status.TERMINATED,NodeState.TERMINATED).build();
}
