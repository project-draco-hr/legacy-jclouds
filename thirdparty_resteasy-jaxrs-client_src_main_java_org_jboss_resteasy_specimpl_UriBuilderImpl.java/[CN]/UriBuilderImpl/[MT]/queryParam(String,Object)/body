{
  for (  Object value : values) {
    if (query == null)     query="";
 else     query+="&";
    query+=Encode.encodeQueryString(name) + "=" + Encode.encodeQueryString(value.toString());
  }
  return this;
}
