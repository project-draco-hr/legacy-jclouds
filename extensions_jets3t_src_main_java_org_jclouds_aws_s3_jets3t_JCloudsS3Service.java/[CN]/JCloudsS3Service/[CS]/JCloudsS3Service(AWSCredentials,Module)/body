{
  super(awsCredentials);
  if (modules == null || modules.length == 0)   modules=new Module[]{new org.jclouds.gae.config.URLFetchServiceClientModule()};
  context=S3ContextFactory.createS3Context(awsCredentials.getAccessKey(),awsCredentials.getSecretKey(),modules);
}
