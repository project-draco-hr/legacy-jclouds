{
  this.locationPredicate=region == null ? Predicates.<Location>or(isZone(),isRegion()) : isZoneOrRegionWhereRegionIdEquals(region);
  this.locationsSupplier=checkNotNull(locationsSupplier,"locationsSupplier");
}
