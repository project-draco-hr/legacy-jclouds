{
  return new Builder().id(id).resourcePath(resourcePath).name(name).description(description).operatingSystem(operatingSystem).location(location).cpuCount(cpuCount).memory(memory).osStorage(osStorage).additionalLocalStorage(additionalLocalStorage);
}
