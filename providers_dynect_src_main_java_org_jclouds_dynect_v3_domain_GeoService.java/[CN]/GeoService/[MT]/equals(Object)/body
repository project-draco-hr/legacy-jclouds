{
  if (this == obj)   return true;
  if (obj == null || !(obj instanceof GeoService))   return false;
  GeoService that=GeoService.class.cast(obj);
  return equal(this.active,that.active) && equal(this.name,that.name) && equal(this.ttl,that.ttl)&& equal(this.nodes,that.nodes)&& equal(this.groups,that.groups);
}
