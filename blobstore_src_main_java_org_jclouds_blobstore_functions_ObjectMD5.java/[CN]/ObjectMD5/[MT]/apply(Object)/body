{
  Blob object;
  if (from instanceof Blob) {
    object=(Blob)from;
  }
 else {
    object=blobFactory.create(null);
    object.setPayload(Payloads.newPayload(from));
  }
  if (object.getMetadata().getContentMD5() == null)   object.generateMD5();
  return object.getMetadata().getContentMD5();
}
