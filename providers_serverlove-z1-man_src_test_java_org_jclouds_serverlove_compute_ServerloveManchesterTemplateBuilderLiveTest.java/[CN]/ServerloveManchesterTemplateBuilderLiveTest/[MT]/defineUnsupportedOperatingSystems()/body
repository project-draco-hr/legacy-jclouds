{
  return new Predicate<OsFamilyVersion64Bit>(){
    @Override public boolean apply(    OsFamilyVersion64Bit input){
      return ((input.family == OsFamily.RHEL) || (input.family == OsFamily.CENTOS && !(input.version.equals("5.5") && input.is64Bit)) || (input.family == OsFamily.UBUNTU && !(input.version.equals("10.10") && input.is64Bit))|| (input.family == OsFamily.WINDOWS));
    }
  }
;
}
