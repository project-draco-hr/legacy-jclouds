{
  setServiceDefaults();
  if (group == null)   group=checkNotNull(provider,"provider");
  if (group.indexOf('-') == -1)   group=group + "-";
  setupKeyPairForTest();
  super.setupContext();
  buildSocketTester();
}
