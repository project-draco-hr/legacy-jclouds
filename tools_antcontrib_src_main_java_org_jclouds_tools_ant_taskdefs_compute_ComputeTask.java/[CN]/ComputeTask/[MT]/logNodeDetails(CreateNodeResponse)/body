{
  log(String.format("   id=%s, name=%s, connection=%s:%s@%s",createdNode.getId(),createdNode.getName(),createdNode.getCredentials().account,createdNode.getCredentials().key,createdNode.getPublicAddresses().first().getHostAddress()));
}
