{
  log("list images");
  for (  Image image : computeService.getImages().values()) {
    log(String.format("   image location=%s, id=%s, version=%s, arch=%s, osfam=%s, desc=%s",image.getLocationId(),image.getId(),image.getVersion(),image.getArchitecture(),image.getOsFamily(),image.getOsDescription()));
  }
}
