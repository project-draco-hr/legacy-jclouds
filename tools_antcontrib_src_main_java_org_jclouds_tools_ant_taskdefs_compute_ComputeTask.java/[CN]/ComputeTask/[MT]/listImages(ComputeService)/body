{
  log("list images");
  for (  Image image : computeService.getImages().values()) {
    log(String.format("   image location=%s, id=%s, name=%s, version=%s, arch=%s, osfam=%s, osdesc=%s, desc=%s",image.getLocation(),image.getId(),image.getName(),image.getVersion(),image.getArchitecture(),image.getOsFamily(),image.getOsDescription(),image.getDescription()));
  }
}
