{
  return new MapMaker().makeComputingMap(new Function<URI,ComputeService>(){
    @Override public ComputeService apply(    URI from){
      return new ComputeServiceFactory(props).create(from,defaultModulesProvider.get());
    }
  }
);
}
