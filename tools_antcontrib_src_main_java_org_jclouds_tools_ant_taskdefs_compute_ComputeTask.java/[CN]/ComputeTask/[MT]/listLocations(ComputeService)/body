{
  log("list locations");
  for (  Location location : computeService.getLocations().values()) {
    log(String.format("   location id=%s, scope=%s, description=%s, parent=%s, assignable=%s",location.getId(),location.getScope(),location.getDescription(),location.getParent(),location.isAssignable()));
  }
}
