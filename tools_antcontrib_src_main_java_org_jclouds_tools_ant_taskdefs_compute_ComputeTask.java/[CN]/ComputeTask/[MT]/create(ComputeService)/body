{
  String tag=nodeElement.getTag();
  log(String.format("create tag: %s, count: %d, size: %s, os: %s",tag,nodeElement.getCount(),nodeElement.getSize(),nodeElement.getOs()));
  Template template=createTemplateFromElement(nodeElement,computeService);
  NodeMetadata createdNode=Iterables.getOnlyElement(computeService.runNodes(tag,nodeElement.getCount(),template));
  logNodeDetails(createdNode);
  addNodeDetailsAsProjectProperties(createdNode);
}
