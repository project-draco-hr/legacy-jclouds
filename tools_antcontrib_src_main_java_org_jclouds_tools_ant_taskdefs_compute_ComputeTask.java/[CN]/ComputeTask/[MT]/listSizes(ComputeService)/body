{
  log("list sizes");
  for (  Size size : computeService.getSizes().values()) {
    log(String.format("   size id=%s, cores=%s, ram=%s, disk=%s",size.getId(),size.getCores(),size.getRam(),size.getDisk()));
  }
}
