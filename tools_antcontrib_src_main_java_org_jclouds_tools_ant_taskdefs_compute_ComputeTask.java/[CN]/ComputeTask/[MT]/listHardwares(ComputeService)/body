{
  log("list hardwares");
  for (  Hardware hardware : computeService.listHardwareProfiles()) {
    log(String.format("   hardware id=%s, cores=%s, ram=%s, volumes=%s",hardware.getProviderId(),getCores(hardware),hardware.getRam(),hardware.getVolumes()));
  }
}
