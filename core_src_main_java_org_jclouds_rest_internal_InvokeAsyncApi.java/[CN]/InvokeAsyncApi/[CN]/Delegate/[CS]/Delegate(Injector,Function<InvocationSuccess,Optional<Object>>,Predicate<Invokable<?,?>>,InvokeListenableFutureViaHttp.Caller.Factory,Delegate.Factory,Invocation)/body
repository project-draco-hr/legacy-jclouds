{
  super(injector,optionalConverter,mapsToAsyncHttpRequest,httpCallerFactory.caller(caller),factory);
  this.string=String.format("%s->%s",caller,caller.getInvokable().getReturnType().getRawType().getSimpleName());
}
