{
  checkState(mapsToAsyncHttpRequest.apply(invocation.getInvokable()),"please configure corresponding async class for %s in your RestClientModule",invocation.getInvokable());
  return Result.success(invokeMethod.apply(invocation));
}
