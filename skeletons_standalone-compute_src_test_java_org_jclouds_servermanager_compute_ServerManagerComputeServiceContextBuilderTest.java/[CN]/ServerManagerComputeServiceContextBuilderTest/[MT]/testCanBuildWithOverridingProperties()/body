{
  Properties overrides=new Properties();
  overrides.setProperty("servermanager.endpoint","http://host");
  overrides.setProperty("servermanager.api-version","1");
  ComputeServiceContext<ServerManager,ServerManager> context=new ServerManagerComputeServiceContextBuilder().overrides(overrides).build();
  context.close();
}
