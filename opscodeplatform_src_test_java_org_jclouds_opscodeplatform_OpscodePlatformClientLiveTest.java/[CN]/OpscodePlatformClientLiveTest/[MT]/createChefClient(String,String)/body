{
  Properties props=new Properties();
  props.setProperty("chef.endpoint","https://api.opscode.com/organizations/" + orgname);
  return (ChefClient)new RestContextFactory().createContext("chef",user,adminKey,ImmutableSet.<Module>of(new Log4JLoggingModule()),props).getApi();
}
