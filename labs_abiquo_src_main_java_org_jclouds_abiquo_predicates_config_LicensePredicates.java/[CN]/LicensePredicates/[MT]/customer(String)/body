{
  checkNotNull(customerIds,"customers must be defined");
  return new Predicate<License>(){
    @Override public boolean apply(    final License license){
      return Arrays.asList(customerIds).contains(license.getCustomerId());
    }
  }
;
}
