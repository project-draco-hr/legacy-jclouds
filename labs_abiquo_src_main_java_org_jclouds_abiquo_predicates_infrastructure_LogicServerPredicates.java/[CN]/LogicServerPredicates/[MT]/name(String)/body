{
  checkNotNull(names,"names must be defined");
  return new Predicate<LogicServer>(){
    @Override public boolean apply(    final LogicServer logicServer){
      return Arrays.asList(names).contains(logicServer.getName());
    }
  }
;
}
