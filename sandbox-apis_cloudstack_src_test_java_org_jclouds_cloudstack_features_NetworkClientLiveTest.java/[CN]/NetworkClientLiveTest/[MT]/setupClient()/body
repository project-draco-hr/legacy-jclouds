{
  super.setupClient();
  try {
    zone=find(client.getZoneClient().listZones(),ZonePredicates.supportsGuestVirtualNetworks());
    offering=Iterables.find(client.getOfferingClient().listNetworkOfferings(),NetworkOfferingPredicates.supportsGuestVirtualNetworks());
    networksSupported=true;
  }
 catch (  NoSuchElementException e) {
  }
}
