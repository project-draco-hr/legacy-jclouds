{
  return source(in.getSource()).vAppScopedLocalId(in.getVAppScopedLocalId()).instantiationParams(in.getInstantiationParams()).networkAssignments(in.getNetworkAssignments()).sourceDelete(in.isSourceDelete());
}
