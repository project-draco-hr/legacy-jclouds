{
  super(awsCredentials);
  context=S3ContextFactory.createContext(awsCredentials.getAccessKey(),awsCredentials.getSecretKey(),modules);
  connection=(S3Client)context.getProviderSpecificContext().getApi();
}
