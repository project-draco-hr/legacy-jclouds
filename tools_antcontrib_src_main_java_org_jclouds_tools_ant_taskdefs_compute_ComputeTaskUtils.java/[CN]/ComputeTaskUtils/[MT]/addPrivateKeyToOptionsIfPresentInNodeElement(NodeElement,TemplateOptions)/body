{
  if (nodeElement.getPrivatekeyfile() != null)   try {
    options.installPrivateKey(Files.toString(nodeElement.getPrivatekeyfile(),Charsets.UTF_8));
  }
 catch (  IOException e) {
    throw new BuildException(e);
  }
}
