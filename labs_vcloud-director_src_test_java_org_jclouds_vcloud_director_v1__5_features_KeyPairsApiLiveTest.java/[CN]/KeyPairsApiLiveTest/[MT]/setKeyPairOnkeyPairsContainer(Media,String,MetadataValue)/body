{
  Task setKeyPair=mediaApi.getMetadataApi(media.getId()).putEntry(keyPairName,keyPair);
  Checks.checkTask(setKeyPair);
  assertTrue(retryTaskSuccess.apply(setKeyPair),String.format(TASK_COMPLETE_TIMELY,"setKeyPair"));
}
