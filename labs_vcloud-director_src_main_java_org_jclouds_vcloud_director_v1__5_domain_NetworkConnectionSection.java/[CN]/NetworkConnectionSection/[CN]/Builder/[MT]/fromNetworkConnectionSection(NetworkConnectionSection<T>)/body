{
  return fromSection(in).primaryNetworkConnectionIndex(in.getPrimaryNetworkConnectionIndex()).networkConnection(in.getNetworkConnection()).link(in.getLink()).any(in.getAny()).href(in.getHref()).type(in.getType());
}
