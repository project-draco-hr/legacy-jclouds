{
  return fromSection(in).primaryNetworkConnectionIndex(in.getPrimaryNetworkConnectionIndex()).networkConnection(in.getNetworkConnections()).links(in.getLinks()).href(in.getHref()).type(in.getType());
}
