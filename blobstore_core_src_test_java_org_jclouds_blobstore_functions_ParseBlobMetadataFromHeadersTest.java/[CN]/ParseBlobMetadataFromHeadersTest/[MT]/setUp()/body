{
  parser=new ParseSystemAndUserMetadataFromHeaders<BlobMetadata>(new DateService(),"prefix",new BlobMetadataFactory<BlobMetadata>(){
    public BlobMetadata create(    String key){
      return new BlobMetadata(key);
    }
  }
);
  parser.setContext(new HttpRequest("GET",URI.create("http://localhost/key")),new Object[]{});
}
