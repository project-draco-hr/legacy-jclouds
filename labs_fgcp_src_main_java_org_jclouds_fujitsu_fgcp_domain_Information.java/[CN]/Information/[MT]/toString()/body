{
  return Objects.toStringHelper(this).omitNullValues().add("seqno",seqno).add("entryDate",entryDate).add("title",title).add("message",message).add("startDate",startDate).add("expiry",expiry).toString();
}
