{
  return fromHostedServiceProperties(in).rawStatus(in.getRawStatus()).status(in.getStatus()).created(in.getCreated()).lastModified(in.getLastModified()).extendedProperties(in.getExtendedProperties());
}
