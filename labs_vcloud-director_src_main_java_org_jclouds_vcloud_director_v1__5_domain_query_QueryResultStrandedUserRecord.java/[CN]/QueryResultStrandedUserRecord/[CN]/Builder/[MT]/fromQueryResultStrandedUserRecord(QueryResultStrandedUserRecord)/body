{
  return fromQueryResultRecordType(in).name(in.getName()).fullName(in.getFullName()).isInSync(in.isInSync()).numberOfDeployedVMs(in.getNumberOfDeployedVMs()).numberOfStoredVMs(in.getNumberOfStoredVMs());
}
