{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  AdminOrg that=AdminOrg.class.cast(o);
  return super.equals(that) && equal(settings,that.settings) && equal(users,that.users)&& equal(groups,that.groups)&& equal(catalogs,that.catalogs)&& equal(vdcs,that.vdcs)&& equal(networks,that.networks);
}
