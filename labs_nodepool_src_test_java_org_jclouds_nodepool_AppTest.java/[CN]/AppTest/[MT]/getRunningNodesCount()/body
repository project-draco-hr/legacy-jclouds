{
  int nodeCount=0;
  for (  ComputeMetadata cm : pooledCompute.listNodes()) {
    if (NodeMetadata.class.isInstance(cm)) {
      String nodeGroup=((NodeMetadata)cm).getGroup();
      if (!((NodeMetadata)cm).getState().equals(Status.SUSPENDED) && !((NodeMetadata)cm).getState().equals(Status.TERMINATED)) {
        nodeCount++;
      }
    }
  }
  return nodeCount;
}
