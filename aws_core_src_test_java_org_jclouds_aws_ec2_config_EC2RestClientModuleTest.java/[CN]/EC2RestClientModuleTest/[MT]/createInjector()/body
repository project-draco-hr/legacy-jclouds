{
  return Guice.createInjector(new EC2RestClientModule(),new ExecutorServiceModule(sameThreadExecutor(),sameThreadExecutor()),new ParserModule(),new AbstractModule(){
    @Override protected void configure(){
      Jsr330.bindProperties(this.binder(),new EC2PropertiesBuilder("user","key").build());
      bind(UriBuilder.class).to(UriBuilderImpl.class);
    }
  }
);
}
