{
  if (request.getPayload() != null) {
    String contentTypeWithVersion=versionAppender.apply(request.getPayload().getContentMetadata().getContentType());
    request.getPayload().getContentMetadata().setContentType(contentTypeWithVersion);
  }
  return request;
}
