{
  checkNotNull(type,"type must be defined");
  return new Predicate<RecordSet>(){
    @Override public boolean apply(    RecordSet zone){
      return type.equals(zone.getType());
    }
    @Override public String toString(){
      return "typeEquals(" + type + ")";
    }
  }
;
}
