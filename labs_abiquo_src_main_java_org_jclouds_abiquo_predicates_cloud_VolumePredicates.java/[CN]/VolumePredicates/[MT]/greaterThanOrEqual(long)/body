{
  checkNotNull(sizeInMb,"sizeInMb must be defined");
  return new Predicate<Volume>(){
    @Override public boolean apply(    final Volume volume){
      return volume.getSizeInMB() >= sizeInMb;
    }
  }
;
}
