{
  if (testContext.getAttribute("jclouds.test.user") != null) {
    amzClient=new AWSAuthConnection((String)testContext.getAttribute("jclouds.test.user"),(String)testContext.getAttribute("jclouds.test.key"),false);
  }
 else {
    throw new RuntimeException("not configured properly");
  }
}
