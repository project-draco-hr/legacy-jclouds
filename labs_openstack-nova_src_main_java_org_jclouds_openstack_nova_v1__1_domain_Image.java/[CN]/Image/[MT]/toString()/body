{
  return toStringHelper("").add("id",id).add("name",name).add("links",links).add("status",status).add("updated",updated).add("created",created).add("progress",progress).add("serverRef",serverRef).add("metadata",metadata).toString();
}
