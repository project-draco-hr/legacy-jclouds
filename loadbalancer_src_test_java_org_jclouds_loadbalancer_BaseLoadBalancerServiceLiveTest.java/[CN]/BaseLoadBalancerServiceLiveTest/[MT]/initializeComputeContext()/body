{
  if (computeContext != null)   computeContext.close();
  computeContext=ContextBuilder.newBuilder(computeProvider).modules(setupModules()).overrides(setupComputeProperties()).build(ComputeServiceContext.class);
}
