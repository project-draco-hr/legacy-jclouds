{
  list.setName(list.getName() + "Updated");
  list.update();
  List<TemplateDefinitionList> lists=env.enterprise.listTemplateDefinitionLists(TemplateDefinitionListPredicates.name("myListUpdated"));
  assertEquals(lists.size(),1);
}
