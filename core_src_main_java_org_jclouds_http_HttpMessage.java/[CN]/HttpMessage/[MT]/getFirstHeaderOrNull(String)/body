{
  Collection<String> values=headers.get(string);
  if (values.size() == 0)   values=getLowercaseHeaders().get(string.toLowerCase());
  return (values.size() >= 1) ? values.iterator().next() : null;
}
