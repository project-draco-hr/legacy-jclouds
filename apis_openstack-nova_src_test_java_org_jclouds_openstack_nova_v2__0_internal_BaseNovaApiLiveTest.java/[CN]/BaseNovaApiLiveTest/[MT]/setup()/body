{
  super.setup();
  zones=api.getConfiguredZones();
  for (  String zone : zones) {
    ServerApi api=this.api.getServerApiForZone(zone);
    for (    Resource server : api.list().concat()) {
      if (server.getName().equals(hostName))       api.delete(server.getId());
    }
  }
}
