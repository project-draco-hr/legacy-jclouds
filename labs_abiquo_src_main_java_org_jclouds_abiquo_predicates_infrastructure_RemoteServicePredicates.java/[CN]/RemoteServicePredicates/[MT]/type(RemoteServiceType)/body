{
  checkNotNull(types,"types must be defined");
  return new Predicate<RemoteService>(){
    @Override public boolean apply(    final RemoteService remoteService){
      return Arrays.asList(types).contains(remoteService.getType());
    }
  }
;
}
