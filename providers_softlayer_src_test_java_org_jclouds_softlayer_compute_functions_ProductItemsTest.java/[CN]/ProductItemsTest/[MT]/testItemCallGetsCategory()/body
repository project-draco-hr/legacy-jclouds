{
  ProductItemPrice price=ProductItemPrice.builder().id(1).category(category).item(item).build();
  ProductItem newItem=item().apply(price);
  assertEquals(newItem.getCategories(),ImmutableSet.of(category));
}
