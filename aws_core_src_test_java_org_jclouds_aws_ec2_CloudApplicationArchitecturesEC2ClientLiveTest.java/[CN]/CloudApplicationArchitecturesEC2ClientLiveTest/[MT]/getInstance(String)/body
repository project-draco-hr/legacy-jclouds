{
  Set<Reservation> reservations=client.getInstanceServices().describeInstancesInRegion(Region.DEFAULT,instanceId);
  return Iterables.getOnlyElement(Iterables.getOnlyElement(reservations).getRunningInstances());
}
