{
  return ImmutableList.copyOf(transform(dtos,new Function<VLANNetworkDto,Network<?>>(){
    @Override public Network<?> apply(    VLANNetworkDto input){
      return wrapNetwork(context,input);
    }
  }
));
}
